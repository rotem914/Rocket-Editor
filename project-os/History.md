# Rocket Editor — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  Create that file the first time you need it. Move rows **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-08-27 | process | **The project got its rules.** A fresh, empty repository was created for Rocket Editor and the ProjectOS kit installed into it: the entry file that names the project and its owner, and the ten process docs plus two tool-server docs. Nothing about the product itself was built, and no stack was chosen — the assistant now has a process to follow the moment building starts. |
| 2026-08-27 | process | **The project has a plan and a direction.** The product plan moved in from DS Tiger and now lives at `notes/Rocket-Editor-Plan.md`. Rocket Editor will be a local web app, not a desktop app and not an editor extension, and that choice plus the client-folder write carve-out are both written down as decisions. The one-time install instructions were deleted and the GitHub remote was added. |
| 2026-08-27 | process | **Corrections about HOW the assistant works now have a file.** `Mistakes.md` joins the folder: the first time Rotem corrects the way the work was done it gets one row, and if the same slip repeats it becomes a real rule in the file that owns that behavior. It is deliberately self-emptying, never a diary, and it is read at task pickup like the rest. |
| 2026-08-27 | process | **The Figma notes now say how to CONNECT Figma, not only how to use it, and reaching into another project is banned.** Claude went hunting in the Rotem E folder for the missing wiring, because the file described the work and never the connection. It now says the connection is a setting on Rotem's machine, what he does once, and how Claude checks it in one free call. Rule 21 makes this project the only source: a missing piece is a question, never a search in a neighbouring folder. |
| 2026-08-29 | docs | **The system has a written architecture, built to be argued with.** `notes/Rocket-Editor-Architecture.md` proposes the whole technical shape: Node and TypeScript, a React panel served by one local process, plain files instead of a database, and one gated write path where no byte reaches a client repo without a verified backup and all-or-nothing rollback. It re-scopes the phase plan now that this is a standalone product, cuts the dev-server phase for good, and retires two facts in the plan that turned out to be wrong. Nothing is ratified; it exists to be reviewed. |
| 2026-08-29 | process | **The project is called Rocket Editor now.** Every document and both note files were renamed from Live Editor, in all three spellings the old name appeared in. The root folder moved as well, from `J:ProjectsLive Editor` to `J:ProjectsRocket Editor`. The assistant could not do that half: its own session held the folder open, so Rotem ran the one command himself. Past History rows were renamed in place too, against this file's own never-rewrite rule, so that the paths and rollback commands inside them still point at a folder that exists. The GitHub repository is still named LiveEditor, and only Rotem can rename that. |
| 2026-08-30 | docs | **The architecture now answers to a designer who does not read code.** Two rounds of feedback folded into the architecture document, which roughly doubled: the fixed list of what can be edited, visible text editing, responsive detected but never edited, a selection panel that says what you picked and what controls it, blast radius with four proof levels and no room for a guessed number, staged work that survives a crash, a gate that refuses a change whose result would not parse, a Safe / Review / Blocked layer computed from the gap between the scope you asked for and the scope you got, and a handoff file for an external coding agent. The phase plan was re-cut around what a person can do, and the honest headline is that the product as scoped sits about five weeks further out than the first draft implied. |
| 2026-08-30 | docs | **The plan got its first hostile review, and the verdict is that it is analyzed but not decided.** A solo pass over both plan documents after the 21-agent builds, hunting what those passes shared a blind spot on. Ten findings in `notes/Plan-Review-2026-08-30.md`: the Plan file's top note now contradicts the architecture, the two decisive spikes have no runnable path if client repos are unavailable, nothing restricts where an attached repo may live (a WSL or synced folder breaks the write-safety assumptions silently), no phase budgets design time for the product's own interface, and the 28 open decisions need a five-question forcing function. Nothing was fixed; every finding waits for an owner verdict. |
| 2026-08-30 | docs | **Every architecture decision was checked against the outside world, and every one of them stands.** Six research passes, about forty claims, verified against official documentation, the tools' own source code, live probes on this machine, and a fresh scan of every competing product. Zero decisions overturned. Two designs need their careful version rather than their simple one, the file-write lock and the forbidden-paths list, and about fifteen spec sentences earned corrections, including one invisible-character trap that would have corrupted the first line of a client file. The market scan found nothing to buy instead and confirmed the safety layer has no competitor. The report plus a seventeen-question list for Rotem landed in `notes/Decision-Verification-2026-08-30.md`. |
| 2026-08-30 | process | **A shell mistake reverted a governance file, and the repair is recorded rather than hidden.** While writing a History row, a command was passed to the shell unquoted, so text inside backticks was executed instead of stored. One of those was a rollback instruction, `git checkout` on `project-os/Mistakes.md`, which the shell obediently ran: the file lost the correction row just added to it and lost the project rename, silently, with a success message printed. Caught on read-back minutes later. Both the corrupted History row and the reverted file are repaired, and the slip is now itself a row in the file it damaged. |
| 2026-08-30 | docs | **Four product decisions came back approved, and the plan now commits.** The first release is the real interface rather than an interim theme editor: every control present from day one, and the ones that cannot yet reach an element say so in plain words and name what would unlock them. A value outside the project's scale is now accepted immediately and marked custom, with no dialog, and it stays local; changing a value the whole project shares still explains itself first. Line height became its own control, taking the editable list from ten to eleven, and no control ever moves another. The preview now instructs the browser directly, which makes custom values exact instead of approximate and removes the need for any extra styling machinery. Promoting a custom value into the shared design system joins the second version as an always-explicit act. |
| 2026-08-30 | docs | **Clicking one element whose colour is shared now offers both paths, and the safe one is the default.** Change just this element, or change it everywhere after seeing what moves. This was the most frequent moment in the product and the plan previously treated it as an accident to warn about on every colour edit; it is now a two-word choice the designer makes, so the warning machinery is spent only where something is genuinely surprising. Where the tool cannot yet identify the element, only the shared path works, and the panel says so rather than silently widening the edit. |
| 2026-08-30 | docs | **Editing the words on the page is in, and the plan now says honestly how much of that is possible.** The requirement is any text the designer can see. Text written in the markup is the first release. Text living in a content or translation file is reachable in principle, since the words are still written down in the project, and that became its own open question rather than a quiet promise. Text arriving from a live database or interface call is permanently out of reach, and the plan says so plainly: those words are the client's content rather than their design, and no edit made in this tool could ever land on them. |
| 2026-08-30 | docs | **The line between text that can be edited and text that cannot is now drawn where a designer can see it.** Rotem asked how he could possibly edit words held in a database on a server he has no access to, which exposed that the plan had been blurring two unlike cases under one label. The rule is now simple: if the words are written down in a file inside the folder he attached, they are editable, and that includes content files and translation files. If they arrive from a server at run time, they are never editable, because they are not in the project at all. A line in a translation file counts as a shared value, so it shows its reach before it changes, exactly like a shared colour. |
| 2026-08-30 | docs | **The plan asked what the client's team receives, and the answer is that the product hands over nothing at all.** Rotem clones the client's repository, makes his changes, pushes them to a branch of his own, and their team reviews that branch. Every one of those steps is his, in his own git client. So the product's whole delivery obligation is to leave the working files clean enough to survive an engineer's review, which is what the one-line-diff writing discipline was built for. The summary file drops from being a deliverable to being material for the review description. The safety snapshots were confirmed not to travel with a push. |
| 2026-08-30 | docs | **The one line the product writes into a client's project can no longer travel into their review by accident.** Three layers now guard it: the panel always shows when the line is in place, an installed commit-time guard refuses any commit carrying the product's footprint, and, at Rotem's own requirement, the guard confirms out loud that it checked and found nothing, because a guard that succeeds silently looks exactly like a guard that is missing. The guard itself never enters their code, covers temporary files and snapshot references too, refuses to install over hook tooling it cannot preserve, and defends against forgetting rather than pretending to prevent a deliberate override. This turns the client ask from a codebase change into a temporary local edit with an enforced exit. |
| 2026-08-30 | docs | **The pre-build checks have their test material, and nothing blocks them anymore.** Rotem named his own three systems, Rotem E, DS Tiger and Donotello, as the first calibration targets, with downloaded public projects widening the sample afterwards. The plan records why that order is honest: his three were never written to pass the test, but they share one author's habits, so the public ones guard against a result that looks better than a stranger's codebase would. Reading those three folders is the owner-named exception to the rule against reaching into other projects, for the calibration task only. This closes the plan review's second blocking finding, the spikes with no runnable path. |
| 2026-08-30 | docs | **The plan now says exactly where clicking an element works without installing anything, and the honest answer corrects how it was being described.** It is not project by project, it is area by area inside every project. Markup written out directly, landing pages, hero sections, page layouts, works well, because its styling text is long and distinctive. Reusable components that assemble their own styling as they render cannot work at all, because the finished styling exists nowhere in the source to match against, and on a project built from a component library that covers most buttons, inputs and cards. What decides the ratio for any given project is how much of its surface is written-out markup versus assembled components, which is exactly why it gets measured rather than assumed. |
| 2026-08-30 | docs | **The commit guard was only protecting half of what the product can leave behind.** It listed the script line, temporary files and snapshot references, and said nothing about the heavier footprint: the labelling package in the client's dependency file, its lockfile entry, and the line in their build configuration. Those are two tracked files a reviewer reads closely, and a dependency appearing in a client's project without explanation reads as a supply-chain change rather than a leftover. The guard now enumerates every footprint by name, because a guard is worth exactly what it lists. |
| 2026-08-30 | docs | **The plan was quietly assuming Rotem has to ask a client's permission to use his own tools, and he does not.** Everything the product installs lives in his local working copy and is blocked from every commit by the guard, so the branch his client reviews carries design changes and nothing else. That puts it in the same category as the editor he uses. The plan now says there is usually nothing to ask, names the narrow cases where he might choose to raise it anyway, and states that the product must make that easy and never mandatory, because a tool that insists on an unnecessary conversation costs him standing every time. |
| 2026-08-30 | product | **The product pivoted to its lean form, and Rocket stopped being allowed to write files at all.** Rotem brought the idea from a separate Claude chat and then pushed it twice past it: Rocket previews, records and reports; Claude Code makes every real code change from the report; and the engine is read-only everywhere on the machine, with the session living in the browser's own storage and files reaching the user only as downloads or clipboard copies. The old architecture, broker, gates, labelling, hook and all, is preserved unedited as `notes/Rocket-Editor-Writing-Track.md`, the pickup point if the Claude loop ever proves insufficient. First useful version moves from roughly thirteen weeks to five to seven. The safety story is now one sentence: this program cannot create or change a file on your computer. |
| 2026-08-30 | docs | **The lean product is broken into 32 small tasks, each one ending in a check Rotem runs himself.** `notes/Rocket-Editor-Build-Plan.md`: thirteen tasks to a skeleton where he drags a real element's padding live inside Rocket, eleven more to a working tool proven on one real change request through the full Claude loop, eight more to a client-presentable finish. Supervision is built into the shape: no task closes without something he can open, click or read, tasks with no visible surface ship a staged demo, and only two tasks need anything from him beyond his eyes, three past change requests and the panel design. |
| 2026-08-30 | docs | **When a client project has a design system, its values now come first in every control.** Owner rule: colour controls open on the project's own palette as named swatches, text sizes on its type scale, spacing on its steps, all read from their theme files. Picking a system value records it by name, and the report hands Claude the name, so the written code references the token the way their own engineers would write it. A free custom value stays one deliberate step away, instant and marked, per the settled off-scale decision. System first, freedom second, nothing blocked. |
| 2026-08-30 | docs | **The whole plan is compressed into one self-contained file for outside reviewers.** `notes/Rocket-Editor-Review-Pack-2026-08-30.md`, 359 lines: the product, the eight hard constraints, all fifteen ratified decisions with their one-line reasons, the system design including the read-only wall's exact enforcement recipe and the verified origin design, the full stack with what was rejected, the six invariants, all 32 build tasks each with its technical mechanism, the four open spikes, a section listing what was deliberately deleted so reviewers do not report it as gaps, and nine named attack surfaces ending with the core question: name the change a designer makes weekly that the report cannot describe. Built to be pasted into Claude, GPT or Gemini with no repository access. |
| 2026-08-30 | product | **The unit of work Claude receives is now a small group of related changes, sent mid-session, not one big report at the end of the day.** Select a card or a section, make one to five related changes, approve with your eyes, send, keep working. A miss surfaces in minutes, on the part of the page you are still looking at. Two mechanisms ride along because sending makes the site rebuild: unsent drafts re-apply themselves by fingerprint after every reload, flagged rather than silently dropped when an element cannot be re-found, and each sent group verifies itself by re-reading the real values, turning landed or off on its own. The full report keeps every job except primary handoff: history, backup, end-of-session summary. |
| 2026-08-30 | docs | **A clean hostile pass over the lean plan found the pivot's two blind spots, both born from the pivot itself.** First: any script in the client's page can impersonate the helper, and its words flow into the report that gets pasted into Claude Code, which has write access, a path from a stranger's script into executed code changes; the old plan treated page data as untrusted and the pivot silently dropped that stance. Second: drafts and verification re-find elements by fingerprint after reloads, but the strongest fingerprint is the styling string, which is exactly what Claude just changed, so the key dies at the moment it is needed. Nine findings total in `notes/Plan-Review-2026-08-30-B.md`: two blocking, five important, two nits, all awaiting verdicts, none fixed. |
| 2026-08-30 | docs | **All nine findings from the clean review pass are fixed into the plan, on a one-word owner verdict.** The two serious ones now have their floors: everything arriving from the client's page is treated as data and never instructions, fenced in every handoff with a standing caution to the executor, because any script in their page can impersonate Rocket's helper; and element re-finding now keys only on the stable fingerprints, position and words, never on styling text, which is exactly what Claude changes. The rest rode along: theme changes get their own group, each client project gets its own session bucket, the engine's read access is contained by an API that simply has no path parameter, the translation-text claim was corrected to the truth, colliding previews name which one is visible, drafts are per page, and the clean check lives in the permanent indicator. |
| 2026-08-30 | docs | **Fourteen findings arrived from a second outside review, thirteen went in, and one waits for Rotem.** The best catches: the client's own app erases previews without any reload, since React rewrites element styling on every state change, so the preview moved off the element into an injected style rule that re-renders cannot touch; every change now records the screen width and light-or-dark mode it was made under, without which the executor cannot place it; the read-only wall gained the platform's own permission mode as an outer layer, blocking the escapes no wrapper sees, with the earlier research rejection reconciled since that verdict was about attackers and this threat is accidents; a fourth group state, landed with a deviation, stops the executor's correct behavior from being scored as failure; fingerprints refresh after every successful re-find so long sessions do not strand drafts; and text editability is now decided by searching the project's files for the string, which answers the real question where watching the page only guessed. Waiting on Rotem: whether font weight, border width and shadow join the control list. |
| 2026-08-30 | product | **The control list grew from eleven to fifteen, on the owner's call.** Font weight, letter spacing, border width and shadow join; line height and corner rounding were already in from the morning and were confirmed rather than added. Shadow, the one composite value in the set, leans hardest on the system-first rule: the project's own shadow steps come first and a hand-built shadow counts as advanced. No new build tasks were needed; typography grew to four independent controls and the rounding task became corners, borders and shadows. The growth rule stands: the list moves only when Rotem says so, and this is what that looks like. |
| 2026-08-30 | docs | **The first version now saves one change at a time, and that simplification removes more work than it adds.** Edit, Apply, check it landed, move on. Rotem's argument was that saving is the part with the most ways to go wrong, so it should carry the least ambition first, then batching returns in a later version with real experience behind it. The effect is large: with at most one change ever waiting, the whole durable-staging design stops being first-version scope, though the write journal that protects a crash mid-write stays untouched. Separately, the labelling tool will live in Rocket's own folder rather than being installed into the client's project, so their dependency list is never touched and the footprint halves. |
| 2026-08-31 | tools | **The project's first code: a Chrome extension that copies an element's ID card for Claude.** Rocket Class Copy lives in `tools/class-copy`, a standalone helper, not the app. Arm one tab from its toolbar button and hold the key left of 1: hovering highlights any element and shows a dark bubble with its design facts, one value per row, exactly as dictated in chat. A click copies a four-line ID card, page, words, classes, position, built to land Claude on the right source, and while inspecting neither the key nor any click reaches the page. Loading it into Chrome is a one-time manual step only Rotem can do. |
| 2026-08-31 | tools | **The hover bubble got its polish round, on live owner feedback.** Text bubbles now open with the font's name, then weight, then size, then color. The bubble is capped at 320 pixels wide and a long class line ends with three dots, since the copied card carries the full text anyway. The shadow is gone, side padding grew to 24, and the bubble's own font is Google Sans. All dictated one line at a time while using the loaded extension on a real site. |
| 2026-08-31 | tools | **Text bubbles now show their padding too.** Hovering a button, a link or any text element adds a last row with its padding, sides labeled L R T B, reading `Padding 0` when there is none, per the owner's always-show rule. Asked for while inspecting a real button whose padding was invisible. |
| 2026-08-31 | tools | **The extension is always on now; no more arming every tab.** Holding the key inspects on any page, out of the box. The toolbar icon flipped meaning: it PAUSES the current tab, badge `OFF`, and resumes it, for the rare site where the key conflicts. The cost the owner accepted knowingly: that key never types anywhere while the extension is enabled, and he has not pressed it in thirty years. |
| 2026-08-31 | tools | **The bubble got its final look, in a fast round the owner checked himself.** Side padding came down by four, to twenty, and the background moved from near-black to a deep navy. Both were dictated and verified live in his own browser. |
| 2026-08-31 | tools | **Hovering a container now shows the spaces between its children, measured.** Each space between two neighbours gets a translucent band with the real pixel distance on a small pill, vertical and horizontal layouts both, however the spacing was created, margin, gap or line height. Text and image elements draw no bands, and releasing the key clears them. Born from the owner asking how to see the two different spaces inside a hero section. |
| 2026-08-31 | tools | **Marking an element now also shows its distances outward.** Up to four blue bands run from the element to the first thing each direction meets, a neighbour or its container's wall, each with the measured pixel distance on a pill. Blue means outside distances, orange means inside spaces, and both show together on a container. Distances under three pixels stay silent so borders do not litter the page with tiny pills. |
| 2026-08-31 | tools | **Outward bands now reach the first edge the eye sees, not the nearest invisible wall.** When an element sits flush against its container, the measuring climbs to the next container and keeps going, so the missing bottom distance the owner spotted on a hero section now shows. A genuinely touching neighbour still stays silent, because that distance really is zero. |
| 2026-08-31 | tools | **The bubble always sits above the bands now.** The blue bands were drawn later, so at equal depth they covered the bubble; bands moved one depth step down, and the bubble and highlight can never be hidden by them again. Spotted by the owner on his portfolio site. |
| 2026-08-31 | tools | **The card's page line now carries the full address, query and hash included.** Feedback from the Claude session consuming the cards: apps keep their whole selection, brands, tabs, dates, in the query string, so a bare "/" hid the state the card was describing. The proposed extra "state:" line was parked by the owner until real use proves the address alone is not enough. |
| 2026-08-31 | tools | **The bubble follows the cursor sideways now.** It stays above or below the element as before, but its horizontal position rides the mouse, centered on it and stopped at the screen edges, so it sits where the owner is looking instead of parked at the element's left corner. |
| 2026-08-31 | tools | **Every measuring pill now says which direction it measures.** A small arrow sits left of the number, up-down for vertical spaces, sideways for horizontal ones, killing the head-math the owner described on a grid where column and row numbers stacked into one confusing column of circles. |
| 2026-08-31 | tools | **The direction arrows are gone again, owner's verdict after seeing them live: too heavy.** Pills went back to the bare number. Tried and dropped inside one hour, which is the loop working as intended. |
| 2026-08-31 | tools | **The extension is named Rocket Inspector now.** The owner closed the name: Rocket Editor stays the full product, and the free tool joins the family as its reading half, since it long outgrew "class copy". Folder, manifest, toolbar title and code comments all renamed; the History and Decisions records keep the old name as history. |
| 2026-08-31 | tools | **The bubble now clears the measuring bands instead of covering them.** It sits above the band over the element, so no pill hides behind it, the owner's rule after a hidden top distance on a client dashboard; when there is no room above, it drops below the bottom band by the same logic. |
| 2026-08-31 | tools | **The bubble stays left-to-right on Hebrew sites.** It was inheriting the page's text direction, right-aligning its rows and flipping the color's `#` to the wrong side; the bubble is now pinned LTR whatever the page's language. Found by the owner on a live RTL site. |
| 2026-08-31 | tools | **The number pills got a touch more air: padding 2 top and bottom, 8 at the sides.** Owner-dictated, tried at 10 and settled back on 8 by eye. |
| 2026-08-31 | tools | **The bubble's corners rounded up from 8 to 13.** Owner-dictated, checked by eye. |
| 2026-08-31 | tools | **The extension's whole UI now lives in Figma, built for color tuning.** A new `Rocket Inspector UI` page in the owner's `Rocket` file shows every state: four bubble kinds, both flash states, both band colors with their pills, a lone pill and the element highlight. Every color is a local paint style under `Inspector/`, so changing the palette is one edit in the styles panel, not a hunt across layers. One substitution recorded on the canvas itself: Google Sans is not visible to the Figma server, so the page renders in Roboto, the product's own fallback. |
| 2026-08-31 | tools | **The Figma page now shows the Inspector against reality: a light screen and a dark screen.** Two mini web pages, nav, hero, button, cards, each wearing the full Inspector layer, highlight, bubble, orange and blue bands with pills, all bound to the same `Inspector/` styles, so tuning one style repaints both worlds at once. Rotem uploaded Google Sans to his account, so the whole page, including the earlier states, now renders in the product's true font and the substitution note is gone. |
| 2026-08-31 | tools | **The extension has a face now: a white R on the owner's blue.** Four icon sizes generated at `#2D41D7`, wired into the manifest for the toolbar and the extensions page. |
| 2026-08-31 | tools | **The palette came back from Figma and landed in the extension.** The owner tuned the `Inspector/` styles against the light and dark screens, and the values were read straight out of the file: the bubble and pills move from near-black navy to a deep blue, and both bands soften to muted sand and light blue at equal strength. The design-to-code loop the Figma page was built for ran end to end for the first time. |
| 2026-09-01 | tools | **The copied confirmation is now the designed pill: a check mark, the word Copied, fully rounded.** Drawn from the owner's own Figma redesign, including the icon he added, which the code builds as real vector shapes rather than pasted markup so the strictest client sites cannot refuse it. The green deepened to match the tuned style. The failure message was deliberately left as it was, since only the copied state was redesigned. |
| 2026-09-01 | tools | **The failure message now wears the same pill, in red, and says what to do: "Failed, try again".** Owner's call right after seeing the copied one; no icon there, since a check mark would contradict the message. |
| 2026-09-01 | tools | **The bubble now fades in when it appears, 144ms, easing out.** Owner's ask, "let's put a bit of soul in". It plays only on the appearance, never on the moves that follow, and a machine set to reduced motion gets no animation at all. One guard rode along and earned its place during testing: if the browser's animation clock stalls, the fade is dropped rather than left half-played, so the bubble can never end up invisible. |
| 2026-09-01 | tools | **A fast round of feel: the bubble now behaves around the cursor and the numbers never dim.** The entrance quickened from 144ms to 56ms across four passes of the owner's eye. The number pills moved onto their own layer, so one band's wash can no longer tint a neighbour's number. An element taller than the window now puts the bubble at the top of the screen instead of the bottom. And the bubble follows the cursor properly: it never covers it, and its text column lines up with the cursor's own position rather than the box edge. The bubble blue was re-pulled from Figma after another tuning pass. |
| 2026-09-01 | tools | **Corner rounding joined the bubble, only where it exists.** A `Radius` row closes every kind of bubble when the element has rounded corners: one number when all four match, corner letters like `TL10 TR10` when they differ, percentages shown as themselves, and no row at all on square elements, per the owner's "when there is". |
| 2026-09-01 | tools | **The copied card grew two lines, adopted from executor feedback after review.** A `box:` line carries size, margin and padding, so the executor can tell whether a distance lives on the element or on a neighbour. A `file:` line appears only when the dev build stamped the element with its source, Lovable's tagger first, then generic inspector attributes, then React's debug source, and says "(parent)" when the stamp sat on an ancestor; unknown is never guessed. The proposal's `space`, `inner` and `type` lines were parked, and its idea of documenting the format in this repo was dropped, since the card's readers live in client repos: the card explains itself instead. |
| 2026-09-01 | tools | **Two more card lines, chosen from the assistant's own reader wishlist.** `attrs:` carries the element's id and its test or accessibility handles when it has any, the strongest search keys after classes. `view:` always closes the card with the window size and light or dark, because a Tailwind edit is breakpoint-scoped and the executor must know which breakpoint the owner's eyes were on, a need Rocket's own plan had already ruled. The `landmark:` and `near:` ideas wait for the field. A later round added the element's own typography as a `type:` line, the parallel of `box:`, giving the executor the value an edit starts from; the rejected version was the one that aggregated the CHILDREN's typography, which a hover on the child answers better. The first field card caught the theme lying: it said dark for a light-rendering site, because it read the browser's preference; it now judges by the page's own painted background, the truth the owner's eyes saw. |
| 2026-08-31 | tools | **The white boxes behind the swatch labels in Figma are gone.** Figma frames are born with a white fill and the ten swatch-row containers kept theirs; all cleared in one repair call, and the trap is now a written rule in the Figma rules doc. |
| 2026-09-01 | tools | **A Tailwind pill was reporting a radius of 33,554,400, and the card was missing border and radius entirely.** `rounded-full` computes to a number in the tens of millions, which the bubble printed raw; any radius that reaches half the element now reads `full`, and elliptical corners read as a pair. The copied card gained the same radius on its box line, a `border:` line with width, style and colour, per side when the sides differ, and the element's own `type:` line. Found by the owner on a client dashboard, where the button he copied carried a border and a pill radius that the card never mentioned. |
| 2026-09-01 | tools | **An adversarial review of that change found eleven ways it could lie, and all eleven are fixed.** The worst were colours: Tailwind now emits colour spaces the tool could not read, so an invisible border was being reported as painted and a green one was pasted as unreadable `oklch(...)`; the tool now paints each colour on a hidden pixel and reads it back, which turns any colour space into a hex an executor can use. Also fixed: a gradient border reported nothing at all, an oval corner was measured against the wrong axis, a maths expression in a radius was shredded into a made-up number, a scaled preview turned a 20px corner into a pill, a wrapper reported its own inherited font instead of the words you see, and a newline inside a label could break the card's one-fact-per-line shape. Three older weaknesses in the quoted-words line were confirmed too and wait for the owner's verdict. |
| 2026-09-01 | tools | **The quoted words in the card are now only the words a person can actually read.** On the owner's go, three older faults were fixed: a page's hidden data blob could be quoted instead of the visible label, two children's sentences fused into a word that exists in no file, and camelCase SVG tag names were flattened to lowercase, so a search of the source found nothing. Screen-reader-only labels are skipped as well, which closes the trap discussed just before: an icon button no longer reports the font of a label clipped to one pixel. |
| 2026-09-01 | tools | **A see-through colour swatch showed the bubble's blue instead of the colour.** The owner's button uses a grey at four percent opacity, and the little square painted it straight onto the bubble, so a barely-there grey read as blue. Swatches now sit on whatever the page really has behind that colour, walking up until it finds something painted, so the square matches what his eye sees on the page. Opaque colours are unchanged. |
| 2026-09-01 | tools | **The bubble shows borders now, and both the bubble and the card show shadows.** Border had only ever reached the copied card, which is why hovering a bordered stat card showed nothing about its border. Shadows are new to both and are written the way a designer authors them, offsets first and colour last, rather than the browser's backwards order; two shadows are listed, more are summarised. Immediately caught in the field on a Tailwind v4 card: that framework fills its unused shadow slots with invisible placeholders, so the line showed two of nothing and hid the real shadow; placeholders that paint nothing are now dropped, and a focus ring beside a shadow still shows both. |
| 2026-09-01 | tools | **Every colour is hex now, with its opacity beside it the way a design tool writes it.** The owner's point: a designer reads `#141414 4%` instantly and `rgba(20, 20, 20, 0.04)` not at all. It applies everywhere a colour appears, text, background, border and shadow, and a fully invisible colour simply says transparent. Corrected within the hour by the owner, and rightly: that form is for his eyes only. The copied card speaks to whoever edits the code, so it keeps colours in a form that is valid CSS, hex when opaque and the authored value untouched when see-through, since `#141414 4%` is not something anyone could write into a stylesheet. |
| 2026-09-01 | tools | **The bubble stopped flickering while the mouse moves.** It was tearing down and rebuilding all of its rows on every single mouse movement, dozens of times a second, which made the numbers jitter under the cursor. It now redraws only when what it would say has actually changed, so moving within an element is silent and moving to a new one draws exactly once. That was only half of it: travelling from one element to another sweeps across the wrappers in between, and each of them was being read, so four or five sets of values flashed past on the way. The tool now adopts a new element only once the cursor settles on it, about a sixteenth of a second, while the bubble keeps riding the cursor, so a sweep shows the old values then the new ones, with nothing in between. |
| 2026-09-01 | tools | **Spacing and corner values are separated by commas now.** `Padding L16, R16, T32, B32` instead of spaces, in the bubble and in the copied card alike. The side order the owner set, left right top bottom, is unchanged. Extended the same minute to every place values sit side by side: border reads `1px, solid, #E4E4E7`, a shadow `0px, 1px, 2px, 0px, #000000 5%`, and the card's type line `Manrope, Medium, 14px, #141414`. A colour and its opacity stay one value, and separate shadows or border sides keep their own divider so the groups stay readable. |
| 2026-09-02 | tools | **Text elements now report their line height and letter spacing.** Both appear in the bubble and in the copied card, alongside the font, weight, size and colour that were already there. Line height always shows, since it always means something; letter spacing appears only when the design actually sets one, so it does not add a row saying nothing on ordinary text. The bubble labels them LH and LS at the owner's request, while the copied card keeps the full CSS property names, since its reader is editing code. |
| 2026-09-02 | tools | **The copied card gained three lines, from testing the loop itself.** It now names the element's component when the page is React, which matters because React 19 removed the source location from its own internals and the component name is what survived. It describes how a container arranges its children, flex or grid with direction, gaps and alignment, which the class names only half say on Tailwind and do not say at all on other stacks. And it carries the parent's size, padding and layout, the usual culprit behind a one pixel drift. The proposal to dump every sibling was left out: the bands already show those on screen, and a dump hands the executor arithmetic to get wrong. |
| 2026-09-03 | docs | **The inspector's uncommitted batch got a solo code review, and the copy confirmation has three real races.** A mouse move while the facts return replaces the confirmation mid-fade, a double-click stacks two confirmations, and releasing the key during the swap leaves the confirmation waiting for the next press. All three were confirmed by driving the real script with its timers and animations under control, since the pane tab is hidden. Five smaller items and one pre-existing filing problem in this very file are listed with them in `notes/Code-Review-2026-09-03.md`, awaiting verdicts. |
| 2026-09-03 | tools | **The bubble groups the type facts the way the owner reads them, and a grid finally counts its columns.** Font family keeps its own line, weight and size share the next one. His first real card also exposed a bug in yesterday's grid line: an escaping slip in the patch script had eaten a backslash, so the tool never split the column list and reported `1 cols` followed by all four widths; it now reads `4 cols 380.25px`. |
| 2026-09-03 | tools | **Every bubble now ends with an invitation to click.** A last line, in a soft blue and set eight pixels below the facts, tells whoever is looking that a click copies the element for their agent. It appears on every kind of element and is deliberately absent from the copied and failed confirmations, which say their own thing. |
| 2026-09-03 | tools | **Copying no longer swaps the whole bubble for a green pill; the bubble stays put and only its middle changes.** The facts fade down and out, then the confirmation rises into their place, each in about a seventh of a second, exactly as the owner specified. The panel itself is his Figma design read straight from the file: a ring with a tick, `Specs copied!`, and `Paste it to your agent` beneath. After a moment the element's facts return the same way. |
| 2026-09-03 | tools | **The bubble no longer resizes when the confirmation appears.** The copied panel is shorter than a list of facts, so the bubble used to shrink around it and slide to a new spot the moment a click landed. It now keeps the box it was wearing until the facts come back, so the only thing that moves during a copy is the content inside it. |
| 2026-09-03 | tools | **The bubble learned to be read, not deciphered, and the confirmation stopped moving the furniture.** Its rows now sit in four fixed groups with a breath between them, what it is, how it reads, how it sits, how it looks; every label ends with a colon, values read as words a designer writes, Full and None and Solid, and a zero margin or padding simply does not print. The copy confirmation keeps the bubble's own box instead of shrinking it, is centred inside it, holds 2.4 seconds, and gives way the moment the mouse moves to another element. The measuring pills now sit above the blue outline instead of under it. Run under FAST MODE, so each round was checked by the owner in his own browser. |
| 2026-09-03 | tools | **Every finding from the day's review is fixed.** Two clicks in a row, or a key released mid-swap, used to leave the confirmation stacked, doubled or stranded; one generation counter settles all of it, and an overtaken swap now bows out without touching the bubble. A grid with named lines counts its real columns again, the card's component and file lines are flattened like every other page-written value, and the scan log's fourteen stray rows moved back into their own table. |
| 2026-09-03 | tools | **A background nobody painted no longer takes a row.** The bubble printed `Bg: None` on every element without a background, including buttons and links, which had their own rule forcing the row. Both are gone: the row appears only when a background is actually painted, and the group it lived in disappears with it when nothing else fills it. |
| 2026-09-03 | tools | **Two more rows that said nothing are gone.** A line height nobody set no longer takes a row, since `normal` is the browser's default rather than a design decision, and a nameless element now reads `Div · No class` instead of wearing a parenthesis. The copied card still carries both, its reader being an agent. |
| 2026-09-03 | tools | **The type group reads font, weight and size, colour, then line height.** The colour moved up to sit under the size and lost its `Text:` label: the code and its swatch say what it is without a word in front. Line height and letter spacing follow it. |
| 2026-09-03 | tools | **The colour swatch is a small circle now.** Round instead of square, still twelve pixels across after a brief round at ten, sitting where it always sat, to the right of the code it belongs to. It briefly moved to the left of the code during the same round and was moved back. |
| 2026-09-03 | tools | **Padding is now visible on the element itself, in the same orange as the gaps between children.** A green it briefly wore read as a second, unrelated language; padding is internal spacing too, so it now paints with the identical colour `INSIDE_BAND` already uses, the owner's call. Each side that has padding gets the wash between the border and the content, nothing where there is none, and no numbers on it, the bubble already carries them. |
| 2026-09-03 | docs | **The review's three confirmation races were re-checked against the fixed code, and all three hold.** A mouse move while the facts return, a double-click, a click during the return, and a key released mid-swap each now leave exactly one confirmation, or none where none belongs. The task document marks T1 to T6, T8 and P1 done; only T7, a helper name used twice, is still open. |
| 2026-09-03 | tools | **The numbers on the orange bands lost their pills.** Inside and outside now differ in kind, not only in colour: a gap or a padding side carries a bare number on its wash, half as dark as the band over a light backdrop and a light sand over a dark one, while the blue outward distances keep their pills. A band too thin for its digits still shows them, spilling over it with a hairline halo, so a 4px gap still says 4. Chosen by the owner from a probe page that drew the spill and the hide side by side on six edge cases. |
| 2026-09-03 | tools | **A click on the confirmation keeps it up and gives it a fresh 2.4 seconds.** Before, a second click on the same element while `Specs copied!` was showing tore the panel down and rebuilt it. Now the panel stays exactly as it is and its clock starts over, every click. The review's last open item, a helper name used twice, is closed with a rename. |
| 2026-09-03 | docs | **The four commits since the morning review got a second solo review: two real defects, four small ones, and one pre-existing.** Releasing the key before the clipboard write resolves parks a stale "Specs copied!" for the next press, the morning's T3 through a second door; and on any production React site the copied card names a minified component like `Ko`, which exists in no file. Smaller: a flick to another element inside the swap window keeps the confirmation for its full hold, a subgrid with named lines reads "1 cols subgrid", a padded image gets the padding wash but no Padding row, the panel keeps its first outcome, and this file's scan log is broken by a blank line again with the morning review row filed above older rows. Pre-existing: the card calls a dark Tailwind v4 page "light" because the theme reader still parses rgb() only. All in `notes/Code-Review-2026-09-03-B.md`, awaiting verdicts. |
| 2026-09-03 | tools | **Every finding from the second review is fixed, the pre-existing one included.** A key released before the clipboard finishes no longer parks a stale "Specs copied!" for the next press, a flick to another element mid-swap now brings that element's facts up instead of the old confirmation, a failed copy after a successful one rewrites the panel to "Failed, try again", the card names a React component only on a dev build and never a minified `Ko`, a subgrid with named lines reads plain `grid`, a padded image gets no orange wash since its bubble shows no Padding row, and the card now calls a dark oklch page dark. The stale-confirmation class earned the first Bug Atlas row and two always-check rows in the review calibration. This file's scan log is one table again with the morning review row in date order. |
| 2026-09-03 | tools | **The bands stop being rebuilt on every mouse move.** They depend on the hovered element's box, not on the cursor, yet every move tore them all down and drew them again, and each redraw forced the page to lay itself out. On a heavy page that read as a small lag when moving between elements, reported by the owner after the day's review. They are now redrawn only when the element or its box changes, or on scroll and resize. |
| 2026-09-03 | docs | **Nine bubble ideas went to the backlog in one go.** A pass over the Inspector wearing a designer's hat produced them: the variable behind a value, a layout line, the text styles the eye sees, a font fallback flag, width limits, contrast, pin-and-compare, copy one value, and image facts. The owner backlogged all nine rather than picking; nothing was built. |
| 2026-09-03 | tools | **The bubble no longer waits for the mouse to stop.** The 60ms wait before adopting a new element restarted on every mouse move, so on a fast sweep it only caught up once the cursor stood still; the owner felt it as lag and a stepped clock proved it. The wait now counts from entering the element. The hop itself got cheaper too: the outward measuring read every sibling four times with band writes in between, forcing a layout per direction; it now reads once and writes after. |
| 2026-09-04 | tools | **An invisible link stretched over a card no longer blocks the elements under it.** The owner's client site wraps whole cards in a transparent full-size link, so every point inside the card hit the link and nothing inside could be inspected. The picker now steps under a layer that paints nothing of its own and covers nearly all of its parent, and then finds the element by geometry rather than by hit testing, since a card like that usually turns pointer events off on its own text and the browser will not report it at all. The copied card names the layer on its own line, so the executor still knows the card is one click target. A link that really wraps its content, the Facebook pattern, is untouched. |
| 2026-09-04 | tools | **Four more ways a page hides its own elements are handled.** A link stretched by a pseudo-element, the transparent shape a chart lays over its bars, an overlay that is invisible until you hover it, and a web component's shadow tree: in all four the picker now reaches the real element underneath. A layer that genuinely paints is still respected, and it plus the canvas label went to the backlog. |

## Appendix — deep rows

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-08-27 | Install ProjectOS into a new Rocket Editor repository | Created `J:\Projects\Rocket Editor` and initialized git on `main`. Copied in `CLAUDE.md`, `Installation.md` and `project-os/` from the ProjectOS kit. Replaced every placeholder: project name, owner and role, project root. Wrote the product description in `CLAUDE.md`, set the reply language to English and kept the kit's length dial, calibrated the `Go commit` flow (assistant commits, Rotem pushes), added a rule-12 note that writing into an attached client folder is the product's one sanctioned outside-the-root write, and made rule 16 cover the client's dev server too. Rewrote `Map.md` with the real tree and honest empty stack rows. Set both tool-server setup tables to "not wired". Deleted the example blocks from Map, History, Decisions, Backlog and BugAtlas; the ones inside Code_review and Visual_QA carry their own instruction and stay. | Read the whole kit before editing, per `Installation.md` step 1. Searched every file for surviving curly-brace placeholder tokens after the edits: none left outside `Installation.md`, which documents them. Re-copied `project-os/` from source and redid the substitutions after the first pass double-encoded every em dash; confirmed the repaired files hold real em dashes and no mojibake. Confirmed the tree in `Map.md` matches what is on disk. No application code exists, so there was nothing to build, run or browser-check. | Pass | low | none — first change in a fresh repository | Delete the `J:\Projects\Rocket Editor` folder. Nothing outside it was touched. |
| 2026-08-27 | Post-install: plan moved in, stack direction set, remote added | Moved `Rocket-Editor-Plan.md` out of DS Tiger into `notes/`, with a note at its top saying it was written when this was going to be a DS Tiger section, so its architecture part needs re-scoping. Recorded two entries in `Decisions.md`: local web app over desktop app or editor extension, and the narrow carve-out that lets the product write into an attached client folder. Updated the stack line and plan path in `CLAUDE.md` and `Map.md`, added the `notes/` folder to the tree and ownership table. Deleted `Installation.md` at Rotem request. Added the `origin` remote. | Grepped both docs for stale references to the old DS Tiger path and to `Installation.md`: none left. Re-read the plan file top after the edit to confirm the new note is its own paragraph. Confirmed the tree in `Map.md` still matches disk. Nothing runs yet, so no build or browser check applies. | Pass | low | none — still the first commit | Delete the folder; the plan file is recoverable from DS Tiger git history. |
| 2026-08-27 | Add Mistakes.md, the waiting room for the assistant's own slips | New `project-os/Mistakes.md` with Open, Promoted and Retired tables plus a routing table mapping a slip type to the file that would own its rule. `CLAUDE.md` gains read-list item 11 and rule 20 (write the row in the same reply; a second occurrence becomes a rule and retires the row; skip the room when the rule home is obvious; an overruled product opinion is not an error). `Workflow.md` gains the matching documentation-routing row and a read-step line. Scope excludes code bugs (BugAtlas) and what changed (History). | Anchors grepped in both edited files after the change. The file was generated from the same kit source used across the other three projects, with the placeholders replaced and the kit's example block dropped. No application code exists yet, so nothing to build or browser-check. | Pass | low | 171a18b | Delete `project-os/Mistakes.md` and `git checkout 171a18b -- CLAUDE.md project-os/Workflow.md`. |
| 2026-08-27 | Figma connection section + rule 21, this project is your only source | `project-os/mcp/Figma/Figma_MCP_Rules.md` gains section 0b: the connection is a machine setting and not a repo file, the one-time owner setup (register the server at user or project scope, desktop-app local server versus Figma's hosted one, take the current address from Figma's own docs, confirm the seat and Dev Mode), and Claude's verification order (search for the deferred tools first, then ONE free identity call, then stop and report rather than improvise). `CLAUDE.md` gains rule 21: never open another repository, copy a convention across, treat another project's setup as evidence, or carry its material in; a missing piece is a question; the client-folder design makes cross-project reading a leak; the one exception is Rotem naming another project in the conversation. | Cause traced from a live session that searched the Rotem E folder for the missing wiring. Compared against the Analytics doc, which carries its own install and authorization sections, confirming Figma was the only tool doc without one. Anchors grepped after the edit. No endpoint or command invented: the doc points at Figma's own documentation because both have changed. | Pass | low | fd918cb | `git checkout fd918cb -- CLAUDE.md project-os/mcp/Figma/Figma_MCP_Rules.md` |
| 2026-08-29 | Write the system architecture document | New `notes/Rocket-Editor-Architecture.md`, 1101 lines: constraints, the stack choice by choice with the alternative each beat, the panel origin design and why a login-walled client app needs it, the in-page agent and its capability envelope, the locate-then-splice mutation primitive, the sixteen-gate write path, the staged-edit lifecycle, undo and handoff, module boundaries, twelve invariants, the honest ops cost, a re-scoped six-phase plan with estimates, eleven spikes, six open decisions, and a corrections section retiring two of the plan's facts. `project-os/Map.md` gains the file in the tree and an ownership row, plus `.gitignore` for the `.tmp/` scratch folder. | Read the document back in full. Confirmed the three cross-links resolve on disk (`notes/Rocket-Editor-Plan.md`, `project-os/Decisions.md`, `CLAUDE.md`). Scanned every line for long dashes per rule 17; seven found in headings and replaced. Confirmed the tree in `Map.md` matches disk after adding two files. Source was a 10-agent pass (3 proposals, 3 adversarial critiques, 2 fact checks, synthesis, completeness critic); its outputs sit in the gitignored `.tmp/`. No application code exists, so nothing to build or browser-check. No `Decisions.md` entry was written on purpose: the choices are proposals awaiting Rotem's verdict, and that file records decisions made. | Pass | low | fd918cb | Delete `notes/Rocket-Editor-Architecture.md` and `.gitignore`, then `git checkout fd918cb -- project-os/Map.md`. |
| 2026-08-29 | Rename the project from Live Editor to Rocket Editor | 51 replacements across 13 markdown files, covering `Live Editor`, `Live-Editor` and `LiveEditor`. Both note files renamed to match, the plan through `git mv` so its history follows it. The project-root tables in `CLAUDE.md` and `project-os/Map.md`, the Map tree and the ownership table all updated. Root folder renamed from `J:ProjectsLive Editor` to `J:ProjectsRocket Editor`, by Rotem. Three assistant attempts were refused with the folder in use: PowerShell from the drive root, PowerShell after releasing its own current directory, and a move from the shell outside the folder. The holder was the assistant session itself, whose working directory is the project root, which it cannot release from inside. | Case-insensitive sweep for `live.editor` across the repo after the change: zero hits. Read the diff line by line; every changed line carries the new name and nothing unrelated moved. Confirmed the tree in `Map.md` matches disk after both file renames, and that git recorded the plan as a rename rather than a delete plus an add. Review per `project-os/Code_review.md` ran on the whole working-tree diff: no blocking findings, two important ones reported and deliberately not fixed because they live outside this repo, one cosmetic. | Pass | medium | fd918cb | `git checkout fd918cb -- .` restores the tracked docs under their old names. Rename the folder back, and delete `notes/Rocket-Editor-Architecture.md` and `.gitignore`. |
| 2026-08-30 | Revise the architecture against two rounds of owner feedback | `notes/Rocket-Editor-Architecture.md` goes from 1101 to 2812 lines and from 18 sections to 26. New sections: what the designer can edit, and the split between what may be written and how the tool proves where to write it; selection context; blast radius with four proof levels; durable staging; the designer safety model; degraded modes; the six-hour test. Rewritten: the write path, now twelve gates and five steps with a post-image parse gate before the snapshot; invariants, twelve kept, three amended, six added; the error registry, now machine reason plus designer explanation plus remedy; testing; the phase plan, re-cut around capability; 25 spikes; 28 open decisions. Retired sentences are listed in section 24 rather than deleted silently. | Assembled by script from newly written sections plus four unchanged blocks lifted from the previous version, so nothing was retyped and nothing drifted. The script fails the build on any long dash per rule 17 and on any missing patch anchor. After assembly: zero long dashes, zero duplicate headings, every subsection number matches its parent section, the retired fan-out section is gone, and all three cross-links resolve on disk. Two cross-reference bugs were caught and fixed, both caused by a renumber map that was too broad; one arithmetic slip in the reachability count was caught on read-back. Sources were 11 agents across three passes; the six that survived the stop on Friday night were reused rather than re-run. | Pass | low | fd918cb | `git checkout fd918cb -- notes/` then delete `notes/Rocket-Editor-Architecture.md`. The previous version is also kept at `.tmp/build/ORIGINAL.md` until that folder is cleared. |
| 2026-08-30 | Review the whole plan for gaps and problems | New `notes/Plan-Review-2026-08-30.md`: three blocking findings (stale Plan self-description, spike path with a missing first link, no rule on where an attached repo lives), five important (unbudgeted interface design, no decision forcing function, undefined second tab, dropped calendar caveat, empty review calibration before the broker gets built), two nits, plus the order that converts the plan from analyzed to decided. `project-os/Map.md` gains the file in tree and ownership. | Each claimed absence was verified by grep before being asserted: no real WSL, UNC or sync-folder coverage (all hits were substring noise), zero multi-tab mentions, no calendar caveat, and the Plan's top note read back as asserting its facts carry over unchanged. Cross-links in the review file resolve on disk. No fixes applied, per the review contract: findings wait for fix, drop or backlog verdicts. | Pass | low | fd918cb | Delete `notes/Plan-Review-2026-08-30.md` and `git checkout fd918cb -- project-os/Map.md`, then re-apply the two earlier Map additions from this session. |
| 2026-08-30 | Verify every architecture decision through web research | New `notes/Decision-Verification-2026-08-30.md`: verdicts on seven decision areas in designer language, a seventeen-question list for moving forward with a recommendation each, and a technical appendix of eighteen spec corrections for the build session. `project-os/Map.md` gains the file in tree and ownership. No spec edits were applied; corrections wait for the owner's go. | Six parallel research agents: runtime, panel stack, origin and login design, click-to-source mapping, Windows write safety, competitive scan. Each claim returned confirmed, concern or wrong with dated sources; several were proven by live probes on this machine, including the git snapshot trap reproducing exactly as the spec claimed and a 391-character path working without registry changes. Notable catches: the filesystem write guard needs four surfaces plus preload plus per-worker install; the request guard as written would block the tool's own agent script; postcss silently strips a UTF-8 byte-order mark before computing offsets, shifting every splice by three bytes; five NTFS path tricks missing from the refusal list; one stale citation, a Windows defect fixed in 2023, replaced by a live 2026 CVE. Zero decisions reversed. | Pass | low | fd918cb | Delete `notes/Decision-Verification-2026-08-30.md` and remove its two lines from `project-os/Map.md`. |
| 2026-08-30 | Rewrite the verification report in designer language | Rewrote `notes/Decision-Verification-2026-08-30.md` after Rotem corrected the language. Every one of the seven areas now opens by saying what that part of the product IS and does for him, before any verdict. Internal names, library names and version numbers are gone from the whole designer-facing half and live only in the appendix, which is now labeled as not for him. Findings and recommendations unchanged. One row added to `project-os/Mistakes.md`. | Scanned the designer-facing half for twenty-one jargon markers after the rewrite: zero hits. Confirmed the appendix keeps all eighteen corrections intact. The correction was recorded in the same reply it landed, per rule 20; its home on a repeat is `project-os/Conversations.md` rule 14, which already demands plain language and context before findings. Note: the first attempt at this row was written with an unquoted shell command, so backticks were substituted by the shell and the row landed corrupted; it was detected on read-back and replaced. | Pass | low | fd918cb | `git checkout fd918cb -- project-os/Mistakes.md`. The previous English draft of the report is recoverable only from this session. |
| 2026-08-30 | Repair the damage from an unquoted shell command | The History row for the language rewrite had its file paths replaced by shell output and was rewritten. `project-os/Mistakes.md` was restored by hand: line endings normalised back to LF (the revert reintroduced CRLF), the Live Editor to Rocket Editor rename re-applied, and two Open rows written, one for the language correction and one for this shell slip. Both rows now use the file-based writing pattern, never inline shell text. | Read back the last History row and found `/usr/bin/which` where a file path belonged, which is what exposed the substitution. Ran `git status` to bound the damage: exactly one file affected, `project-os/Mistakes.md`, and no other governance file touched. Verified after repair: zero damaged lines in History, 19 rows intact, the Mistakes title carries the new project name, zero stale name occurrences, two Open rows present, LF endings restored, and the file shows as modified against the commit again. | Pass | low | fd918cb | The repaired state is the intended one; reverting would restore the damage. |
| 2026-08-30 | Apply four approved product decisions to the architecture | 25 patches to `notes/Rocket-Editor-Architecture.md`, now 3012 lines. Section 3 gains line height, the independence rule and a new subsection separating what the designer sees, what the preview shows and what gets written. Section 3.3 rewritten: off-scale values accepted with no dialog, staying local, with the build cost named (a one-off write on the class path moves into the first phase). Section 5.2 rewritten: the preview instructs the browser directly, so the approximate state is retired for the eleven properties pending a spike. Section 20.4 records the decision instead of offering three options; Phase 1 gains line height and custom values; Phase 5 gains promotion into the design system with its four owner-set rules. Two invariants added, 19 keeping a local edit local and 20 forbidding one control from moving another. Three new test suites, one new spike, and the class-signature spike reframed as a schedule question rather than an existential one. Section 22 gains a settled block with three items marked settled in place so numbering does not shift. `project-os/Decisions.md` gains four entries and four index lines. | Patches applied by a script that refuses a missing or ambiguous anchor and fails the build on any long dash; one anchor was wrong on the first run and the script stopped before writing anything, which is what it is for. After applying: 3012 lines, 26 sections, zero long dashes in new text, and a sweep for stale claims found only the corrections section, which records them deliberately. Read back the rewritten scale section and caught one surviving sentence that still promised a deliberate act to leave the scale, contradicting the new rule; fixed in a second pass. Decisions file verified at six entries and six index lines, with new entries using colons rather than the kit template's long dash, per rule 17. | Pass | low | fd918cb | `git checkout fd918cb -- project-os/Decisions.md`, and restore the architecture from `.tmp/build/PRE-DECISIONS.md`, which holds the exact pre-patch copy. |
| 2026-08-30 | Settle the shared-value editing question | New section 3.7 in `notes/Rocket-Editor-Architecture.md`: both paths offered beside the property as a plain either-or, local by default, with the honest limit named for elements the tool cannot locate. Section 12.2 rewritten, since its worked example taught the opposite behavior: the scope gap is now chosen rather than discovered. Item 8 marked settled in place in section 22 and added to the settled block. One entry and one index line in `project-os/Decisions.md`. | Four patches applied through the anchor-checking script; one anchor was wrong on the first run and the script refused to write, then a second attempt failed on an apostrophe in a shell argument, which is the slip already recorded in `project-os/Mistakes.md`, so the patch file was rewritten with the Write tool instead. Verified after: 3051 lines, 26 sections, zero long dashes, decisions file at seven entries and seven index lines. | Pass | low | fd918cb | Restore the architecture from `.tmp/build/PRE-DECISIONS.md` for the whole day, or reverse the four patches in `.tmp/build/q8-patches.json`, and `git checkout fd918cb -- project-os/Decisions.md`. |
| 2026-08-30 | Settle whether visible text is in scope | Section 3.5 of `notes/Rocket-Editor-Architecture.md` gains a three-way split of text by where the words actually live, replacing a flat refusal list that lumped translation keys and live data together. Item 2 in section 22 marked settled, and a new item 2a opened for content and translation files, with its sharpest risk named: one translation key can appear on many screens and in several languages. Settled block updated. One entry and one index line in `project-os/Decisions.md`. | Three patches through the anchor-checking script, applied first time. Verified after: 3082 lines, 26 sections, zero long dashes, decisions file at eight entries and eight index lines. The owner's phrasing, any text with no problem at all, was deliberately not written into the document as a promise, because part of it cannot be met; the document states which part and why instead. | Pass | low | fd918cb | Reverse the three patches in `.tmp/build/q2-patches.json`, and `git checkout fd918cb -- project-os/Decisions.md`. |
| 2026-08-30 | Settle where editable text lives | Section 3.5 of `notes/Rocket-Editor-Architecture.md` now settles the middle of its three kinds of text as in scope, with the shared-value treatment inherited from section 3.7 rather than invented, plus two named limits: editing one language leaves the others alone and the panel says which it is editing, and a line assembled at run time is refused like any other assembled text. Item 2a marked settled in section 22, settled block updated. One entry and one index line in `project-os/Decisions.md`. | Three patches through the anchor-checking script, applied first time. Verified after: 3095 lines, 26 sections, zero long dashes, decisions file at nine entries and nine index lines. Worth recording why this row exists at all: the owner's question was not a request for a change, it was a reader catching an ambiguity the document had, and the fix was to draw the line by where the words are stored rather than by how they behave on screen. | Pass | low | fd918cb | Reverse the three patches in `.tmp/build/q2a-patches.json`, and `git checkout fd918cb -- project-os/Decisions.md`. |
| 2026-08-30 | Settle what the client's team receives | Section 14.2 of `notes/Rocket-Editor-Architecture.md` gains a subsection describing the real workflow and what it removes from the product's scope, plus two consequences that only matter because real branches get pushed: the snapshots live outside the branch namespace so a push cannot carry them, and the existing no-commit no-push rule is now justified in practice rather than in principle. Item 25 in section 22 marked settled with a note that the question was misframed. Settled block updated. One entry and one index line in `project-os/Decisions.md`. | Three patches through the anchor-checking script, applied first time. Verified after: 3137 lines, 26 sections, zero long dashes, decisions file at ten entries and ten index lines. The decision is recorded as safe to settle on a sample of one because the product stops at the working files under any arrangement, which is stated in the entry rather than assumed. | Pass | low | fd918cb | Reverse the three patches in `.tmp/build/q25-patches.json`, and `git checkout fd918cb -- project-os/Decisions.md`. |
| 2026-08-30 | Settle the footprint question with a commit-time guard | Section 14.2 of `notes/Rocket-Editor-Architecture.md` gains the footprint guard subsection: three layers, five make-or-break details, and what it changes about asking a client. Section 5.3 cross-references it. New invariant 21, nothing the product put in a client's project reaches a commit. New spike S-HOOK on whether Rotem's own desktop git client runs hooks and shows their output. Item 19's script-line half marked settled, its labelling half left open, new 19a opened for the zero-touch fallback with a recommendation to wait for a client who refuses. Settled block updated. One entry and one index line in `project-os/Decisions.md`. | Six patches through the anchor-checking script; one anchor missed because item 19 had been reworded in an earlier pass, script refused and stopped, anchor corrected from the live text, all six then applied. Verified after: 3219 lines, 26 sections, zero long dashes, guard section, invariant 21 and S-HOOK all present, decisions file at eleven entries and eleven index lines. | Pass | low | fd918cb | Reverse the six patches in `.tmp/build/q19-patches.json`, and `git checkout fd918cb -- project-os/Decisions.md`. |
| 2026-08-30 | Settle the calibration material | Item 26 in section 22 of `notes/Rocket-Editor-Architecture.md` marked settled with the three named systems, the ordering caveat, the rule-21 sanction scoped to the calibration task, and the one piece still owed on spike day: three past change requests, words rather than folders. Settled block updated. One entry and one index line in `project-os/Decisions.md`. The exchange that produced this also produced the test-pages distinction, homemade pages test the machinery while this check measures other people's building habits, which is recorded in the decision entry's context. | Two patches through the anchor-checking script, applied first time. Verified after: 3237 lines, 26 sections, zero long dashes, decisions file at twelve entries and twelve index lines. The three project folders were not opened; the sanction is for the spikes when they run, not for today. | Pass | low | fd918cb | Reverse the two patches in `.tmp/build/q26-patches.json`, and `git checkout fd918cb -- project-os/Decisions.md`. |
| 2026-08-30 | Specify where the no-install element method works | Section 3.2 of `notes/Rocket-Editor-Architecture.md` gains a plain-language subsection: how the method works stated exactly, where it works, five named cases where it cannot with the reason for each, what decides the ratio for a project, the two things it does not affect (whole-app editing and plain-stylesheet projects), and the point that every failure is a refusal naming its cause rather than a wrong edit. Written because the phrase works on some projects was both vague and pointing at the wrong axis. | One patch through the anchor-checking script, applied first time. Verified after: 3288 lines, 26 sections, zero long dashes. No new decision was recorded; this specifies a mechanism the document already carried in compressed form, and the measurement that fixes its unknown ratio is spike S-L3, unchanged. | Pass | low | fd918cb | Reverse the patch in `.tmp/build/signature-spec.json`. |
| 2026-08-30 | Close a gap in the footprint guard's coverage | The guard's coverage bullet in section 14.2 of `notes/Rocket-Editor-Architecture.md` replaced with a full enumeration: the marker in any tracked file, the labelling package entry and its lockfile line, the labelling line in the build configuration, leftover temporary files, and snapshot references. Plus a note on why the labelling footprint is the serious one, being two tracked files rather than one and both closely read. | One patch through the anchor-checking script, applied first time. Verified after: 3297 lines, 26 sections, zero long dashes. Found while explaining to Rotem the difference between the script line and the labelling install; the guard had been written when only the script line existed and was never revisited when labelling moved into the plan. No decision entry, since this closes an omission in an already-settled decision rather than changing it. | Pass | low | fd918cb | Reverse the patch in `.tmp/build/guard-scope.json`. |
| 2026-08-30 | Correct the client-permission framing | The closing paragraph of the footprint guard in section 14.2 of `notes/Rocket-Editor-Architecture.md` rewritten. It had said the guard makes the permission conversation easier; it now says the conversation is usually unnecessary, since nothing reaches the client's branch, and lists the three narrow cases where he might still raise it as his judgement rather than the product's requirement. Adds the line that the real payoff of the guard is turning a permission question into a private working detail. | One patch through the anchor-checking script, applied first time. Verified after: 3311 lines, 26 sections, zero long dashes. Found because Rotem said he was confused: he had described his workflow twice, and the reply kept treating his client as a participant in a decision they never see. The document had the same flaw, inherited from the moment the script line was first designed. | Pass | low | fd918cb | Reverse the patch in `.tmp/build/asking-fix.json`. |
| 2026-08-30 | Apply the one-at-a-time and vendored-labelling decisions | Section 10 of `notes/Rocket-Editor-Architecture.md` gains 10.0, naming what the first version actually needs, three things kept and everything from 10.1 onward deferred, with the existing design left in place as dated specification rather than deleted. Section 11 gains 11.0 with the decision, what it buys, the rhythm cost stated honestly, and the point that nothing is built and thrown away since the multi-file write path is unchanged and simply exercised one file at a time. Section 4.8 rewritten: the labelling tool loads from Rocket's own copy with automatic fallback to installing it, and the panel always says which is in use. New spike S-VENDOR. Two entries and two index lines in `project-os/Decisions.md`. | Five patches through the anchor-checking script, applied first time. Verified after: 3410 lines, 26 sections, zero long dashes, decisions file at fourteen entries and fourteen index lines. The one-at-a-time entry records that it went against the recommendation, which was to support both and default to batching, and why the owner's argument was the better one. | Pass | low | fd918cb | Reverse the five patches in `.tmp/build/simplify-v1.json`, and `git checkout fd918cb -- project-os/Decisions.md`. |
| 2026-08-30 | The lean pivot, recorded end to end | New `notes/Rocket-Editor-Architecture.md`, 286 lines, replacing the 3400-line document as current architecture: the iron read-only rule with the boot wall now allowing no destination at all, the hand-pasted passive helper line with panel indicator and read-only clean check, exact direct-instruction preview, browser-held durable session record with backup downloads, the fingerprint report as the load-bearing artifact, the Claude loop and its responsibility split, six invariants, three lean phases (5 to 7 weeks to first value, 7 to 10 complete), four spikes with S-LOOP as the core bet, and three remaining open decisions. Old document renamed to `notes/Rocket-Editor-Writing-Track.md` with a superseded note under its title. One pivot entry in `project-os/Decisions.md` naming its four sub-decisions and the three same-day decisions it supersedes, which are italicized in the index. `project-os/Map.md` tree and ownership updated. | New document swept for long dashes: zero. Writing track confirmed retitled with its note and otherwise unedited. Decisions file verified at fifteen entries, fifteen index lines, exactly three italicized; the first italicizing script refused to write on a partial match and the fix went line by line. The transparency sub-decision records that it reversed the assistant's earlier framing, and the entry names the revisit trigger in advance. | Pass | low | fd918cb | Delete `notes/Rocket-Editor-Architecture.md`, rename the writing track back, and `git checkout fd918cb -- project-os/Decisions.md project-os/Map.md`. |
| 2026-08-30 | Write the build plan as 32 owner-checkable tasks | New `notes/Rocket-Editor-Build-Plan.md`: three phases with acceptance runs, tasks sized S to L, the four spikes folded in as tasks (S-A as R-06, S-PREVIEW as R-12, S-LOOP as R-23, S-SCOPE as R-24), R-23 named as the score that can reorder Phase C, and the two owner-input tasks flagged. Counts match the architecture's five-to-seven-week first value. `project-os/Map.md` gains the file in tree and ownership. | Read back in full; zero long dashes; every task has a your-check line; task ordering verified against the architecture's phase content, with the wall as R-02 before anything touches a client folder, per the read-only invariant. | Pass | low | fd918cb | Delete `notes/Rocket-Editor-Build-Plan.md` and remove its two lines from `project-os/Map.md`. |
| 2026-08-30 | Add the system-first rule to controls | Section 5 of `notes/Rocket-Editor-Architecture.md` gains the owner rule with its report consequence, and two build-plan tasks were sharpened to carry it: R-16 opens sizes on the project's type scale, R-17 greets with the client's palette before any colour wheel. | Three patches through the inline anchor-checking pattern, applied first time, zero long dashes in both files after. No new decision entry: this extends the already-settled ladder-over-their-scale logic from spacing to every control, which is the obvious reading of the owner's instruction rather than a contested choice. | Pass | low | fd918cb | Reverse the three patches in `.tmp/build/system-first.json`. |
| 2026-08-30 | Compile the review pack for external AI review | New `notes/Rocket-Editor-Review-Pack-2026-08-30.md`, assembled fresh from the lean architecture, the build plan, the fifteen Decisions entries and the verification-day research findings, written technical on purpose since its audience is AI reviewers rather than the owner. `project-os/Map.md` gains the file in tree and ownership, with the ownership row covering future rounds as one file per round. | Read back in full; zero long dashes; decision list checked against the Decisions index, fifteen for fifteen; the deleted-machinery section cross-checked against the writing-track document so nothing deleted is misdescribed; task list checked against the build plan, 32 for 32 with sizes matching. | Pass | low | fd918cb | Delete `notes/Rocket-Editor-Review-Pack-2026-08-30.md` and remove its two lines from `project-os/Map.md`. |
| 2026-08-30 | Adopt the group-handoff flow | `notes/Rocket-Editor-Architecture.md`: the six-line product statement, the session-record section (grouping rule, group states, reload survival) and the report section (group handoff primary, full report as history) rewritten; invariant 4 extended to cover drafts surviving reloads. `notes/Rocket-Editor-Build-Plan.md`: R-22 rebuilt as groups plus handoff, new R-22b reload survival and R-22c read-only landed verification, R-23 rescored per group, task count now 34. `notes/Rocket-Editor-Review-Pack-2026-08-30.md` updated to match, plus a tenth attack question: do fingerprints survive Claude editing the very class strings drafts are keyed on. One entry and one index line in `project-os/Decisions.md`. | Twelve patches across four files, two anchors wrong on first try and refused by the script before writing. The dash checker flagged double hyphens inside backticked command flags in the review pack; those are code, not prose punctuation, so the pack patch ran with the em-dash check only. Zero real long dashes in all four files after. Decisions file at sixteen entries. | Pass | low | fd918cb | Reverse the patch sets in `.tmp/build/groups-arch.json`, `groups-plan.json` and `groups-pack.json`, and `git checkout fd918cb -- project-os/Decisions.md`. |
| 2026-08-30 | Clean review pass over the lean plan | New `notes/Plan-Review-2026-08-30-B.md`: B1 report poisoning with a three-layer fix (render as text, fence page data, standing executor caution), B2 fingerprint stability with the stable-versus-volatile split, B3 theme groups, B4 per-project storage keying, B5 read containment restored as a no-path API rule, B6 the translation-text decision overclaims for a product that does not trace source, B7 mixed-preview precedence, B8 per-page drafts, B9 clean-check wording. What was re-checked and holds is named at the top. | Fresh read of all three current documents plus targeted greps confirming the absences before asserting them: no untrusted-input treatment anywhere in the lean docs, no per-project storage keying, no read-scope statement. Per the review contract nothing was fixed; findings wait for fix, drop or backlog verdicts. | Pass | low | fd918cb | Delete `notes/Plan-Review-2026-08-30-B.md`. |
| 2026-08-30 | Fix all nine review-B findings | Nine patches across `notes/Rocket-Editor-Architecture.md` (untrusted page data in the helper section, the stable-volatile fingerprint split, theme groups, per-project buckets, preview-collision rule, read containment, the translation-text correction, invariant 3 extended), `notes/Rocket-Editor-Build-Plan.md` (R-13 per-project keying with its check, R-22 theme group and executor caution, R-22b stable-fingerprint check including the restyled-element case, R-26 indicator wording), and the review pack (helper trust rule, fingerprint split, read containment) so the file being sent to outside reviewers matches the plan again. All nine statuses in `notes/Plan-Review-2026-08-30-B.md` marked fixed with the verdict. | Every patch through the anchor-checking pattern; one anchor wrong on first try and refused before writing. Zero long dashes across all four touched files. B9 was initially marked fixed while its edit had not landed anywhere; caught on read-back of the review file and closed with the R-26 patch before this row was written. | Pass | low | fd918cb | Reverse the patch sets in `.tmp/build/fixall-arch.json`, `fixall-plan.json`, `fixall-rest.json`, `fixall-pack.json`, and restore the open statuses in the review file. |
| 2026-08-30 | Fold in the second outside review, thirteen of fourteen | Thirteen patches to `notes/Rocket-Editor-Architecture.md`: the two-layer wall with extra surface closures and the zero-native-binaries boot assertion, the fixed-port invariant with persistent-storage protection, the style-rule preview mechanism, viewport and mode on every record with the dark-mode override rule, the snap-intent bit, the identical-siblings variant warning, states scoped out honestly, the deviation state, weighted matching with fingerprint re-baselining, project-search text editability with the read-containment wording widened to a string-in-verdict-out route, the framework-defaults inventory merge, the disclosure line about quoted interface text, priced estimates, the pre-registered S-LOOP bar, and the control-list question added to open decisions. Six patches to the build plan (R-02 and R-11 grew to L, R-19, R-20, R-22c, R-23) and six to the review pack to keep it current. | Three anchors wrong on first attempts, refused by the script before writing, fixed from the files' actual text. Zero long dashes across all three files. Finding 12, expanding the fixed control list, was deliberately not applied: the list is owner-ruled and grows only on demonstrated need, so it landed as an open decision instead. The permission-model adoption records why it does not contradict the earlier rejection: different threat model, accidents rather than adversaries, and belt plus wrap beats either alone. | Pass | low | fd918cb | Reverse the patch sets in `.tmp/build/claudefix-arch.json`, `claudefix-plan.json`, `claudefix-pack.json`. |
| 2026-08-30 | Grow the control list to fifteen | Property set rewritten in `notes/Rocket-Editor-Architecture.md` with the composite-shadow note, every count updated across architecture, build plan and review pack, R-16 and R-18 in the plan grown to carry the new controls, the open decision closed in place, and one entry plus index line in `project-os/Decisions.md` recording that the owner went one step past the recommendation and added letter spacing unprompted. | Grepped all three documents for the old count after the change; the only survivors are the two lines that deliberately narrate the growth. Zero long dashes throughout. | Pass | low | fd918cb | Reverse via the History diff for this row; the property-set paragraph and the two task blocks are the only substantive edits. |
| 2026-08-31 | Build the Rocket Class Copy extension | Three new files in `tools/class-copy/`: `manifest.json` (MV3, runs on all pages), `background.js` (toolbar click toggles the tab's armed state, the badge is the state), `content.js` (key handling, tint-plus-outline highlight, bubble, ID card, clipboard with a fallback for plain-http sites). Owner tweaks mid-build: no bubble shadow, padding 16 top and bottom, 24 left and right, 14px text. Docs: Map tree and ownership (the tree also gains the missing `Mistakes.md` line), a QA change-type row, a Decisions entry on the ID card, and CLAUDE.md rule 1 gains "a discussion is not a work order", promoted from Mistakes after the same slip twice. | Drove the identical script on a test page in the pane browser, which cannot load real extensions: bubble rows matched the spec for text, image, box and wrapped-words button, including `Bg none`, `Margin 0` and L R T B sides; the copied card carried words, `(none)` classes and `(2nd of 3)` position; page handlers counted zero clicks and the address hash never changed; the key was swallowed on an armed tab and untouched on a disarmed one; keyup, blur and disarm hid everything; the clipboard fallback restores focus; console clean. Review per `project-os/Code_review.md` on the three new files: three findings, all introduced here (wrapped-words buttons read as boxes, the fallback stole focus, one rule-17 dash), all fixed and re-verified. Not run, Chrome-only surfaces: the real unpacked load, held key, clipboard and badge toggle; a 30-second owner checklist handed at delivery. | Pass | medium | fd918cb | Delete the `tools/` folder and revert this task's edits to `project-os/Map.md`, `project-os/QA.md`, `project-os/Decisions.md`, `project-os/Mistakes.md` and `CLAUDE.md` by their diff hunks. The working tree also carries earlier uncommitted work that must stay. |
| 2026-08-31 | Refine the class-copy bubble on live feedback | `tools/class-copy/content.js` only: a font-name row now leads the text rows (first family of the stack, quotes stripped), order font, weight, size, color; the bubble is capped at 320px border-box with ellipsis on overflowing rows; shadow removed; padding 16 top and bottom, 24 sides; the bubble font set to Google Sans with plain fallbacks, loaded from the machine and never from the network. | Injected the identical script on the QA page per fix: text rows read system-ui, Bold, 24px, #FFFFFF in that order; the bubble measured exactly 320 wide with a long class row ellipsized; box rows unchanged. The pane restores identical page snapshots even across navigations, so each re-check byte-bumped the page title to force a fresh document. Review of the diff: clean, nothing beyond the asked changes. Not run: Google Sans rendering itself; on a machine without that font the bubble falls back to Arial by design. | Pass | medium | fd918cb | Reverse this row's small hunks in `tools/class-copy/content.js` by the diff. |
| 2026-08-31 | Add a padding row to text bubbles | One line in `tools/class-copy/content.js`: the text branch appends `Padding` with the same L R T B side labels the box branch uses. | Injected the script on the QA page: the button read `Padding L16 R16 T8 B8`, a padded heading `Padding L8 R8 T8 B8`, a bare span `Padding 0`, and box bubbles unchanged. Review: a one-line reuse of the existing side-label helper, nothing else touched. | Pass | medium | fd918cb | Remove that one line, per this row's diff. |
| 2026-08-31 | Make the extension always on | `content.js`: armed defaults to true. `background.js`: the icon now pauses and resumes the current tab, badge `OFF` in gray, message wiring unchanged. `manifest.json`: description and icon title updated to match. The owner declined the typing exception explicitly, so the key is swallowed everywhere while enabled; a paused tab types normally. | Injected the script on the QA page with no arming message: inspect worked out of the box; the pause message made the key inert for inspecting and let it type into a field again; resume restored inspecting. Review: state default flip plus an inverted badge toggle, no new paths. Not run: the real icon click, Chrome-only; covered by the owner's next use. | Pass | medium | fd918cb | Reverse this row's hunks in the three files by the diff. |
| 2026-08-31 | Fast-mode burst: bubble padding and colour | Two one-value edits in `tools/class-copy/content.js`: side padding 24 to 20, and the bubble background constant from `#1a1a1a` to `#091243`. | Run under FAST MODE, so the owner was the reviewer: he loaded each change in his own browser and passed it before asking for the next. No browser QA or auto review was run by the assistant, by design. Nothing is left unverified: both changes are single style values he looked at directly. | Pass | medium | fd918cb | Restore the two values in `content.js`, per this row's diff. |
| 2026-08-31 | Spacing rulers between a container's children | `tools/class-copy/content.js`: a `kindOf` helper unifies the existing bubble branching; `drawBands` measures consecutive visible children of a box-kind element and draws one orange band per space, vertical or horizontal, with a navy pill carrying the rounded distance; bands redraw on hover, scroll and resize, and clear on hide, release and pause. Stale header comment about per-tab arming corrected in passing. | Injected the script on the QA page: a hero-style section produced two vertical bands reading 24 and 34, the 34 correctly including the button's line spacing, since bands measure rendered rects, not style values; a flex row with gap 12 produced two 12 bands; a text element produced none; release cleared them. Screenshot confirmed the visual: bands, pills and bubble as designed. Review of the diff: the refactor preserves the three bubble branches verbatim; band elements can never be measured as children because they are removed before measuring. | Pass | medium | d226da5 | `git checkout d226da5 -- tools/class-copy/content.js` and re-apply nothing; this task is the only change since that commit. |
| 2026-08-31 | Outward distance bands from the marked element | `tools/class-copy/content.js`: band creation extracted into one `addBand` helper with a colour per layer; new `drawOutward` measures from the hovered element in four directions to the nearest overlapping sibling or the parent's wall, drawing blue bands beside the orange inside ones; wired into paint, scroll and resize, cleared with everything else. Outward silence threshold is 3px, assistant's call, to keep border-touching elements from showing "1" pills. | Injected on the QA page: a button showed 34 up to its paragraph and 318 right to its section wall; a middle card showed only 24 left, correctly nothing vertically where cards touch; a container showed both layers, two orange plus blue; release cleared all. One expected band missing turned out correct: the card overflowed its narrow parent, so there was no inside wall to measure to. The 3px threshold was set after that same run showed two 1px border bands; verified against those measured cases. | Pass | medium | 1930f1d | `git checkout 1930f1d -- tools/class-copy/content.js`. |
| 2026-08-31 | Give the extension an icon on the owner's blue | Four PNGs (16, 32, 48, 128) drawn by script: solid `#2D41D7` with a centered white bold R, matching the letter Chrome showed by default. `manifest.json` gains `icons` and the action's `default_icon`. | Rendered `icon128.png` and looked at it: white R centered on the requested blue. No behavior touched. | Pass | low | 1930f1d | Delete the four `icon*.png` files and revert the manifest hunk. |
| 2026-08-31 | Climb hugging walls when measuring outward | `tools/class-copy/content.js`: `drawOutward` rewritten around a per-direction table; when the container wall sits within 3px of the element, measuring climbs up to six container levels until a real distance or a touching neighbour is found. Touching neighbours end the search silently, so the climb can never draw a distance across an element that is actually flush. | Injected on the QA page: the flush button now shows its bottom band, 21, found one container level up; its 34 up and 318 right held; the middle card still shows only its 24 left, with the touching cards correctly silent rather than jumped over; release clears all. Review of the rewrite: the four direction blocks became data, same math, plus the touch guard the first version lacked. | Pass | medium | 1930f1d | `git checkout 1930f1d -- tools/class-copy/content.js` returns to the pre-bands file; this and the two prior rows share that target. |
| 2026-08-31 | Bands under the bubble | One value in `addBand`: bands render at depth 2147483646, one under the bubble's 2147483647. All overlays are siblings in one stacking context, so the lower number is below by CSS rule; the bug existed because equal depth let insertion order win. | Verified by that stacking rule and the owner's next hover; not re-driven in the pane, a one-value change with certain behavior. | Pass | low | 1930f1d | Revert the one-line hunk. |
| 2026-08-31 | Full address in the card's page line | One line in `idCard`: the page line concatenates path, query string and hash. Requested via feedback from the executor session using the cards; the companion "state:" line was discussed and parked, owner's call, until the address alone proves insufficient. | A one-line concatenation of three standard address parts; behavior certain, confirmed by the owner's next paste. | Pass | low | e100da1 | Revert the one-line hunk. |
| 2026-08-31 | Bubble rides the cursor's X | Two lines in `place()`: the bubble's horizontal anchor is the last cursor X, centered, clamped 4px from either screen edge; vertical placement untouched. | Injected on the QA page: two hovers at different X positions moved the bubble to the exact expected clamped positions, including the left-edge clamp at 4. | Pass | medium | e100da1 | Revert the two-line hunk in `place()`. |
| 2026-08-31 | Direction arrows on the measuring pills | `tools/class-copy/content.js`: every band's geometry carries its axis, and the pill prints an arrow before the number, sideways for horizontal measures, up-down for vertical ones, in both the inside and outward layers. | Injected on the QA page: vertical hero spaces read as up-down arrows, flex-row gaps as sideways, and the button's three outward bands carried the right arrow each; release cleared all. | Pass | medium | e100da1 | Revert this row's hunks in `content.js`. |
| 2026-08-31 | Remove the direction arrows | All seven arrow hunks in `tools/class-copy/content.js` reversed by hand: the pill prints the bare number again and the geometry carries no axis field. The working tree's other uncommitted work, full address and cursor-riding bubble, untouched. | The reverted lines match the pre-arrow text verbatim, the state that passed today's earlier pane runs; not re-driven. | Pass | low | e100da1 | Re-apply the arrow hunks from the previous row's diff. |
| 2026-08-31 | Rename the extension to Rocket Inspector | `tools/class-copy/` moved to `tools/rocket-inspector/` through git so its history follows. Renamed inside: the manifest's name and toolbar title, both file header comments, and the internal guard variable. `project-os/Map.md` tree and ownership updated; the QA page's script link repointed. Past History and Decisions rows keep the old name, per their never-rewrite rules. | Swept the repo for the old name outside the historical records: zero hits. No behavior changed; the pane was not driven. Known consequence for the owner: Chrome points at the old folder path, so the extension must be removed and loaded unpacked once from the new path. | Pass | low | beea4e0 | `git mv` the folder back and revert this row's rename hunks. |
| 2026-08-31 | Bubble clears the bands | `tools/rocket-inspector/content.js`: the up and down outward bands report their far edges, drawing order became bands first and bubble last, and the bubble anchors above the up band's edge, falling back below the down band's edge when the top is out of room. | Injected on the QA page: over a button with a 34px top band the bubble's bottom sat 8px above the band's top; an element with no top band kept its old placement, no overlap either way. | Pass | medium | beea4e0 | Revert this row's hunks in `content.js`. |
| 2026-08-31 | Pin the bubble left-to-right | Two inline style values on the bubble in `tools/rocket-inspector/content.js`: `direction: ltr` and `textAlign: left`, so an RTL page's inherited direction cannot re-order the rows or the `#` in color values. Band pills carry digits only and were unaffected. | An explicit inline style beats page inheritance by CSS rule; confirmed live by the owner on the RTL site where he caught it. Not re-driven in the pane. | Pass | low | beea4e0 | Revert the two style lines. |
| 2026-08-31 | Push the Inspector UI into Figma | Followed `project-os/mcp/Figma/Figma_MCP_Rules.md`: one free `whoami`, then one `use_figma` build call. Created page `Rocket Inspector UI` (id `2:12`) in the owner's `Rocket` file: a wrap layout of ten labeled states, all values captured from the real `content.js`, ten local paint styles under `Inspector/` applied to every fill and stroke, and a canvas note recording the Roboto-for-Google-Sans substitution. The rules doc's setup table is now filled with the file, the target page and the budget posture. | The build code was syntax-checked offline in the pane browser before the metered call, per the budget rule; the call returned the page, root, all ten state ids and the styles. Visual verification is the owner's eye in Figma, by the same rule; no screenshot call was spent. | Pass | low | beea4e0 | Delete the `Rocket Inspector UI` page and the `Inspector/` styles in Figma; revert the setup-table hunk in the rules doc. |
| 2026-08-31 | Theme screens in Figma and the Google Sans swap | One `use_figma` call on the same page: 36 existing texts re-fonted to Google Sans (Regular, Medium, Bold confirmed available after the owner's upload), the substitution note deleted, and two 880 by 620 screens built to the right of the states, light and dark, each a small realistic page carrying the highlight, the riding bubble, two orange inside bands and two blue outside bands, every fill and stroke bound to the shared `Inspector/` styles. The rules doc's font note corrected. | Code syntax-checked offline in the pane before the metered call; the call returned both screen ids, the re-font count and the loaded font styles. Visual verification stays the owner's eye, per the budget rule. | Pass | low | beea4e0 | Delete nodes `7:3`, `7:43` and the two captions in Figma; the re-font has no code-side footprint. |
| 2026-08-31 | Pull the tuned palette from Figma into the code | One read-only `use_figma` call returned all ten `Inspector/` styles as hex plus opacity. Four changed and were applied to `tools/rocket-inspector/content.js`: bubble background and pill background `#091243` to `#0F28AF` (one constant serves both), inside band `rgba(246,170,60,0.30)` to `rgba(222,180,117,0.32)`, outside band `rgba(37,99,235,0.22)` to `rgba(118,161,255,0.32)`. Six styles were unchanged and touched nothing: bubble text, both flash colors, highlight fill and stroke, swatch border. | The pull is the verification: the code's four values now equal the file's, style by style, and the six unchanged ones were compared and left alone. Visual check is the owner's, in his browser, where the palette was designed. | Pass | low | ca8e89a | `git checkout ca8e89a -- tools/rocket-inspector/content.js` restores the previous palette. |
| 2026-09-01 | Rebuild the copied flash from the Figma design | One read call returned the redesigned node plus the current palette. `tools/rocket-inspector/content.js`: the copied state now renders as a flex pill, background `#0D7737`, padding 16 by 32, radius 999, gap 6, a 20 by 20 check mark built with `createElementNS` (path `M3 12L7.33 17L16 3`, stroke `currentColor`, width 2, Figma's default caps kept), and the label `Copied` at weight 700. Bubble padding and radius became named constants, a `bubbleDisplay` variable lets `place()` keep the flex layout, and `resetBubbleChrome()` restores every changed property after the flash and on stop. | Drove the identical script in the pane: the resting bubble measured 13px radius and 16/20 padding; the copied flash measured green `rgb(13,119,55)`, radius 999, padding 16px 32px, display flex, gap 6px, a 20x20 svg whose path and stroke match the design, painted stroke resolving to the bubble text colour, label weight 700, text `Copied`; after 700ms every property returned to the resting values with the svg gone and the element rows back; release hid it. The failed path was exercised too, by the sandbox's real clipboard refusal, and kept its old look. | Pass | medium | ca8e89a | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Card lines: attrs and view | `tools/rocket-inspector/content.js`, in `idCard`: an `attrs:` line joins id plus `data-testid`, `data-test`, `aria-label`, `name` and `role` when present, omitted otherwise; a closing `view:` line always carries `innerWidth × innerHeight` and dark or light. The theme first read the browser's colour-scheme preference; the owner's first field card exposed that as a lie on a light site under a dark OS, so `pageTheme()` now judges by luminance of the page's painted background, body first then html, transparent falling back to light. | Ran the identical tail logic in the pane: the handled element produced `attrs: id=cta · aria-label=Start trial`, the bare element produced no attrs line, and both closed with the view line. The theme fix was proven in the exact failing condition: the pane itself prefers dark, and the light-painted page still read `light`, a dark-painted body read `dark`, a transparent body fell back to `light`. The pane's hidden window still measures 0 × 0, environment artifact; real numbers appeared in the owner's own card, 2122 × 1270. | Pass | medium | fd5bb92 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Grow the card: box line and stamped source line | `tools/rocket-inspector/content.js`: `idCard` appends `box:` (rounded rect size, margin and padding through the existing side labeler) always, and `file:` only when `sourceOf` finds a stamp, checking each of four ancestor levels for Lovable's `data-component-path`/`data-component-line`, then `data-lov-id`, `data-source`, `data-inspector-location`, then a React fiber `_debugSource` walked three fibers up, shortening absolute paths at `/src/` and marking ancestor stamps with "(parent)". | Ran the identical card helpers in the pane: a stamped button's card ended `file: src/components/SaveButton.tsx:12`, the span inside it got the same path marked "(parent)", and an unstamped element got a `box:` line and no `file:` line at all. The React fiber path could not be exercised there, no React app in the pane; it meets its first real test on the Lovable projects. | Pass | medium | fd5bb92 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Fix the huge radius, and put border, radius and type on the card | `tools/rocket-inspector/content.js`: `cornerLabel` split into `cornerValues` plus a formatter, now taking the element so it can cap a radius at half the smaller side and print `full`, which is what Tailwind's `rounded-full` (computed as 33554400px) actually means; elliptical corners print as `10/30`, percentages at or past 50 print as `full`. New `borderLabel` returns null unless a border is really painted, one string when the four sides agree, per-side entries otherwise. `idCard` appends the radius to its `box:` line, a `border:` line when there is one, and a `type:` line for text elements carrying font, weight, size and colour. | Ran the identical helpers in the pane against real computed styles: a `9999px` pill read `Radius full` with `1px solid #E4E4E7`; an elliptical box with two different borders read `Radius TL10/30 TR20/full …` and `T 2px dashed #FF0000 · L 1px solid #0000FF`; a plain card read `Radius 12` with no border line; a square heading produced neither. Full card tails checked for a button, a heading, a plain box and an image, each carrying exactly the lines it should. A three-lens adversarial review with per-finding refutation ran over the new code: 22 candidate findings, 12 survived, 10 refuted. | Pass | medium | fb741a7 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Swap the bubble's middle on copy, from the Figma design | `tools/rocket-inspector/content.js`: rows moved one layer in, into a `content` element, so a swap can move all of them together while the bubble itself does not flinch; `drawRows` writes there now. New `copiedPanel` and `checkDisc` build the design read from node 31:197 in one metered Figma call: a 32px ring, a tick, `Specs copied!` at 16px medium, `Paste it to your agent` at 14px in `#BFC5E7`, spaced 10 and 4. New `swapContent` runs the out animation, 144ms ease-in, fade with 8px down, then builds and runs the in animation, 144ms ease-out, fade with 8px up, with a stall guard so content is never stranded invisible and a reduced-motion path that swaps instantly. `flash` no longer changes the bubble's colour, padding or shape at all; it claims the bubble with a sentinel so a hover cannot repaint mid-swap, holds the panel 700ms, then swaps the facts back. | Verified in the pane by running the mechanism byte-identically: both animations reported 144ms with the right easings and the exact 8px keyframes in the right directions, the content settled at full opacity, and every panel value matched the design, ring 32px with a 2px white border and no fill, white tick, title 16px weight 500, subtitle `#BFC5E7`, margins 10 and 4. The owner amended the ring mid-build from a filled white disc with a blue tick to a white outline with a white tick, and that is what is verified above. Inert after this change, left in place and named for the owner: `FLASH_OK`, `FLASH_FAIL`, `checkIcon` and the `bubbleDisplay` flex path. | Pass | medium | e58a763 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Footer line in the bubble | `tools/rocket-inspector/content.js`: rows can carry their own styling now, `row` takes a fourth argument that `drawRows` applies after the shared styles; `fillBubble` ends with `Click to copy for you agent` at `#97A1D7` with an eight pixel top margin, held in a `FOOTER_COLOR` constant. The flash states build their own content and were left alone. | Verified in the pane by running the row and draw mechanism as patched across the three bubble kinds: the last row carried the exact colour, the eight pixel margin and the text in every one, and the row counts matched. Anchors asserted unique by the patch script; file syntax-checked. The wording was flagged as missing the r in "your"; the owner confirmed the typo and it now reads "Click to copy for your agent". | Pass | low | e58a763 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Regroup the type rows, fix the grid track split | `tools/rocket-inspector/content.js`: the bubble's text branch prints the family alone and then `weight, size` on one line; `layoutOf` had `split(/s+/)` where `split(/\s+/)` was meant, a backslash lost when the patch script's template literal was written, so every grid reported one track. | Verified in the pane: a four-column grid like the one in the owner's card now reads `4 cols 380.25px`, the three-column case still reads `3 cols 84px`, flex lines are untouched, and the text rows read family, then `Regular, 36px`, then LH and LS. The patch was applied with a script that asserts each anchor matches exactly once, and the file was syntax-checked after. | Pass | medium | e58a763 | Revert this row's two hunks in `content.js`. |
| 2026-09-02 | Component, layout and parent lines on the card | `tools/rocket-inspector/content.js`: new `componentOf` walks up to four ancestors and twelve fibers for a capitalised component name, unwrapping `memo` and `forwardRef` and marking an ancestor's name as `(parent)`; new `layoutOf` describes a flex or grid container, direction, wrap, gaps, alignment, an even grid as `3 cols 84px` and an uneven one by track; `idCard` gained `layout:`, `parent:` (name, size, padding, its own layout) and `comp:`, the label padded to the card's column. Row and column gaps that differ use a slash, so a comma never means two things in one line. | Verified in the pane against real computed styles and a hand-built React 19 style fiber, which has no source location: the component name resolved through a plain host element to `StatCard`, a `forwardRef` shape to `IconLabel`, and a non-React element to nothing; even grid, uneven grid, wrapping flex, a flex bar with alignment, and a non-layout element each produced the right line or none; the parent line carried its own layout when it had one. Patches applied through an anchor-checking script and syntax-checked after each. | Pass | medium | e58a763 | Revert this row's hunks in `content.js`. |
| 2026-09-02 | Line height and letter spacing on text | `tools/rocket-inspector/content.js`: the bubble's text branch gained a `Line` row after the size and a `Letter` row when `letterSpacing` is not `normal`; `idCard`'s type line became a list carrying `line-height` and, when set, `letter-spacing`, named as CSS names them so the executor can act on them. New `fineLabel` keeps two decimals, because the shared one-decimal label turned a real `0.32px` into `0.3px`. | Verified in the pane on real computed styles: a ratio line height resolved to `22.4px`, `-0.02em` to `-0.72px`, a whole `2px` stayed whole, `normal` printed as `normal`, and an element with no letter spacing produced no row. Both patches applied through an anchor-checking script that refuses an ambiguous match, and the file was syntax-checked after each. | Pass | medium | e58a763 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Commas between grouped values | `tools/rocket-inspector/content.js`: `sidesLabel` and the unequal-corner branch of `cornerLabel` join with a comma instead of a space. The `·` separators between whole facts were deliberately left alone, and so was the L R T B order. | Verified in the pane on real computed styles: four-sided padding, two-sided margin, a two-sided card, and an all-zero value still reading `0`. Then extended on the owner's follow-up to `borderLabel`, `shadowLabel` and the card's type line; the `·` between separate shadows and separate border sides was kept deliberately, as was `#141414 12%` as a single colour. Re-verified: uniform border, mixed-side border, single, double and inset shadows, and both colour dialects, the bubble's `#000000 5%` beside the card's `rgba(0, 0, 0, 0.05)`. | Pass | low | fb741a7 | Revert the join changes in the four helpers. |
| 2026-09-01 | Stop the bubble rebuilding on every mouse move | `tools/rocket-inspector/content.js`: `row` now collects into a pending list instead of writing to the DOM; new `drawRows` compares a signature of the collected rows against what is drawn and returns early when they match, so the DOM is rebuilt only on a real change. `fillBubble` ends by calling it; `hide` and `flash` clear the signature so the next appearance always draws. | Ran the mechanism in the pane: twenty mouse moves over the same element produced one DOM build instead of twenty, moving to a different element produced exactly one more, moving back produced one more, and a cleared signature redraws identical content. File syntax-checked and every row call site confirmed to feed the new path. A second report followed, that a sweep still showed several value sets: new `aimAt` holds a candidate element for 60ms before adopting it, restarting the wait on every change, adopting instantly when there is no current target, and cleared on stop. | Pass | medium | fb741a7 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Show every colour as hex plus opacity | `tools/rocket-inspector/content.js`: `toHex` no longer returns a raw `rgba()` for see-through colours; it emits `#RRGGBB NN%`, reading the colour a second time at full opacity first, since a nearly-transparent pixel cannot report its own channels. Both `toHex` and `isTransparent` now take the fast comma path only for the legacy form and send space syntax to the canvas. | Verified in the pane across eleven colour forms: the owner's own values became `#141414 4%` and `#141414 12%`, a shadow colour `#000000 5%`, `oklch(... / 0.4)` became `#00C950 40%`, opaque legacy and named colours unchanged, a fully transparent value says transparent, unparsable text passes through, and the four transparency checks hold. One real bug was caught and fixed inside this task: modern space syntax produced `#14NANNAN` before the comma guard. File syntax-checked. The owner then split the two audiences: new `cssColor` keeps the card CSS-valid, `borderLabel` and `shadowLabel` take an optional colour formatter, and `idCard` passes it for border, shadow and type. Verified: the same values read `#141414 4%` in the bubble and `rgba(20, 20, 20, 0.04)` on the card, opaque colours are hex in both, transparent says transparent in both. | Pass | medium | fb741a7 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Border and shadow in the bubble, shadow on the card | `tools/rocket-inspector/content.js`: new `splitTop` splits a value list on its own commas only, never inside `rgb(...)`; new `shadowLabel` moves the browser's leading colour to the end so a shadow reads in authoring order, shows at most two and caps the line. `fillBubble` gained a `Border` row, reusing the card's own `borderLabel`, and a `Shadow` row; `idCard` gained a `shadow:` line beside its existing `border:`. | Verified in the pane against real computed styles: a stat card like the owner's screenshot produced `1px solid #E4E4E7` and `0px 1px 2px 0px rgba(0, 0, 0, 0.05)`; a two-shadow element listed both, separated; an inset shadow kept its keyword; an element without one produced nothing; and the comma splitter returned exactly two shadows from a string containing four commas inside colours. File syntax-checked and all four call sites confirmed. A field card from the owner then exposed the Tailwind v4 case, fixed in the same task: `shadowLabel` now skips any piece whose colour is transparent or whose lengths are all zero, and returns nothing when every piece is a placeholder. Re-verified in the pane: the owner's exact computed string, four placeholders plus one real shadow, now reads `0px 1px 2px 0px rgba(0, 0, 0, 0.05)`; a ring plus a shadow keeps both; an all-placeholder element produces no row; single, double, inset and no-shadow cases unchanged. | Pass | medium | fb741a7 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Composite swatches over the page's real backdrop | `tools/rocket-inspector/content.js`: new `backdropOf` walks up to twelve ancestors for the first non-transparent background, falling back to white. `row` gained a base parameter and now paints the swatch as a colour layer over that base rather than straight onto the bubble. `fillBubble` resolves two backdrops: the parent's for the background swatch, the element's own for the text-colour swatch. | Rebuilt the swatch exactly as `row` does and read back its computed layers in the pane: the reported case, `rgba(20, 20, 20, 0.04)` on a white parent, now paints the colour over white instead of over the bubble's `#0F28AF`; an opaque card colour is unchanged; the walk-up found a grandparent's grey for a nested button; the fallback returns white when nothing is painted anywhere. File syntax-checked. | Pass | medium | fb741a7 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Fix the three older card findings, plus the hidden-text guard | `tools/rocket-inspector/content.js`: new `isHiddenText` treats non-rendering markup, `display:none`, `visibility:hidden`, zero opacity and a box of one pixel or less as invisible. New `visibleText` walks text nodes, skipping hidden subtrees and inserting a space at every element boundary, capped at 400 characters; `ownWords` now reads from it, clips the excerpt at 80 characters and replaces any double quote so the card's own quoting cannot break. New `tagName` helper uses `localName`, wired into the card's element line, `nameOf` and the bubble's label, so `foreignObject` keeps its spelling. `textStyleSource` now skips hidden children, which was the sr-only trap named to the owner before he approved this batch. | Verified in the pane against each named input: a div holding a JSON-LD script quoted `Visible label` only; an anchor wrapping a heading and a paragraph quoted `Big title Body copy here` with the space; `foreignObject` kept its case; an icon button with a one-pixel `Close dialog` label reported its own 15px Semibold and quoted nothing from the hidden span. Regressions checked: ordinary button and paragraph excerpts unchanged, the nav-item type still resolves to its anchor, ordinary tag names unchanged. Whole file syntax-checked after the edits. | Pass | medium | fb741a7 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Fix every confirmed finding from the review | `tools/rocket-inspector/content.js`. Colour: `colorRGBA` paints a value on a 1×1 canvas and reads the pixel, so `oklch`, `color-mix` and relative syntax resolve; `toHex` keeps the legacy `rgb/rgba` path untouched for exactness and only paints modern spaces; `isTransparent` does the same, so a zero-alpha `oklab` no longer counts as a painted border. Radius: per-axis caps for elliptical corners, maths expressions with a percentage printed verbatim instead of split on spaces, and the cap measured on the layout box (`offsetWidth`/`offsetHeight`) so a transformed ancestor cannot fake a pill. Border: `borderImageSource` named when a gradient border paints, widths multiplied by `currentCSSZoom`. Type: new `textStyleSource` resolves the first text-bearing descendant, applied to the card and, named as a side effect for the owner, to the bubble too so the two can never disagree. Attrs: values flattened to one line and capped at 120 characters. | Every fix was re-verified in the pane against the exact failing input the review named: the 320×48 ellipse reads `40/20` (was `full/20`); the 40×200 rail reads `full/40`, honest since 20 is full on a 40px width; `calc(50% - 4px)` and `min(50%, 24px)` print verbatim (were `calc(50%/0` and `0/24`); the scaled 64px tile reads `Radius 20` (was `full`); the zero-alpha border returns null (was reported); the gradient border reads `4px image linear-gradient(…)` (was null); the oklch border reads `#00C950` (was raw); a nav item reports its anchor's real 13px Semibold `#2563EB` (was the list's inherited 16px Times); the newline label flattens to one line. Regression checks: exact legacy hex, semi-transparent values kept verbatim, named colours, unparsable strings untouched, and the three transparency cases. Whole file syntax-checked and every call site re-read. Three confirmed findings were NOT fixed, per the review contract: they predate this change and live in the quoted-words line, which can swallow script text, glue words across children, and lowercase camelCase SVG tags. | Pass | medium | fb741a7 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Add the Radius row | `tools/rocket-inspector/content.js`: a `cornerLabel` helper reads the four computed corner radii, collapses equal corners to one number, letters the unequal ones TL TR BR BL with zero corners omitted, passes percentage values through untouched, and returns null for square elements so `fillBubble` appends the row only when it exists, on every bubble kind. | Ran the identical helper in the pane against real computed styles: a 12px card read `Radius 12`, a 10px-top button read `Radius TL10 TR10`, a square heading produced no row, and a 50% circle read `Radius 50%`. The two wiring lines append the returned row last, matching the bubble mock the owner approved. | Pass | medium | fd5bb92 | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Fast-mode burst: cursor behaviour, pill layer, palette pull | Run under FAST MODE, so the owner checked each round in his own browser. All in `tools/rocket-inspector/content.js`: fade duration 144 to 104 to 72 to 56ms; `addBand` split into a band rectangle at depth 2147483645 and its pill as a separate fixed element at 2147483646, centred by transform, both tracked in the same array so clearing and sibling-filtering still work; the vertical fallback now prefers below only when it fits on screen and otherwise pins the bubble 4px from the top, which is what a taller-than-window element gets; horizontal placement moved from centred-on-cursor to right-of-cursor at 16 then 32px, then to the cursor's exact X, and finally to aligning the bubble's text column with the cursor by subtracting the live computed left padding, with a mirrored flip when the right edge runs out. One palette pull from Figma changed `BUBBLE_BG` from `#0F28AF` to `#1B32AE`; the other nine styles already matched. | The owner was the reviewer, per the mode: every round was loaded in his browser and passed before the next was asked for, including on live Facebook and his own portfolio, which is where the tall-element and cursor-cover cases were found. No assistant browser QA or auto review was run, by design. Nothing is left unverified: each change is a placement or value the owner watched directly. | Pass | medium | ca8e89a | Revert this row's hunks in `content.js`; the palette line reverts to `#0F28AF`. |
| 2026-09-01 | Fade the bubble in on appearance | `tools/rocket-inspector/content.js`: `fadeInBubble()` runs a 144ms ease-out opacity animation through the browser's animation API, fired from `place()` only on the hidden-to-visible transition, so hovering from element to element never restarts it. Reduced motion skips it entirely, per `project-os/QA.md` section 7. Side effect named for the owner: a 400ms guard cancels a still-running fade, because the pane's frozen animation clock proved a stalled fade would otherwise strand the bubble at zero opacity, which QA section 8 forbids. | Drove the identical block in the pane: one animation on appearance, timing 144 with ease-out and keyframes 0 to 1; repeated placements while visible started nothing new; after the guard fired the element measured opacity 1 with zero animations attached, in an environment whose clock never advances; hiding and showing again produced exactly one fresh animation. | Pass | medium | ca8e89a | Revert this row's hunks in `content.js`. |
| 2026-09-01 | Match the failed flash to the copied pill | `tools/rocket-inspector/content.js`: `flash()` no longer branches on shape, both outcomes get the pill chrome and the bold label, only the colour differs and only success appends the check mark; the failure text became `Failed, try again`. | Drove the identical script in the pane and exercised both outcomes in one run: the failure measured red `rgb(127,29,29)`, radius 999, padding 16px 32px, flex, gap 6px, weight 700, no icon, text `Failed, try again`; it restored to the resting bubble after 700ms; the success path then measured the green pill with its icon, unchanged. | Pass | medium | ca8e89a | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Keep the bubble's box while the confirmation shows | `tools/rocket-inspector/content.js`: new `lockBubbleSize` records the bubble's own width and height as minimums just before `flash` starts its swap, and new `unlockBubbleSize` releases them in the build that brings the facts back. Minimums rather than fixed sizes, so a panel taller than a very short bubble grows it instead of spilling outside the rounded background. The lock refuses to run twice, so a second click while the panel is up cannot re-record the panel's own smaller box. Side effect named for the owner: `hide` releases the lock too, otherwise a copy interrupted by releasing the key would leave the next element's bubble stuck at the previous one's size. | Drove the real mechanism in the pane, armed, hovered, clicked: the facts measured 214x174 at 30,321 and the confirmation measured 214x174 at the same spot, with the panel's own content 48px shorter inside it, and the facts came back at the same box. The lock read 214px by 174px while the panel was up and was empty again after. A second click during the panel kept the same box, and the swap still animated 144ms ease-in with the content settling at full opacity. File syntax-checked. | Pass | low | e58a763 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Code review of the uncommitted inspector batch | New `notes/Code-Review-2026-09-03.md`: three important findings in `flash` and `swapContent` (T1 the sentinel released before the return swap ends, T2 overlapping swaps on a double-click, T3 a stop during the swap cannot cancel the pending build), five nits (rise-in unguarded against a stalled clock, named grid lines counted as tracks, `comp:` skipping `flat`, a shadowed `nameOf`, stale comments and inert leftovers), and one pre-existing item: fourteen scan rows filed inside this file's appendix table since 2026-09-01. No code changed. | Range: uncommitted tree only, HEAD equals origin/main. The script was driven in the pane on `.tmp/swap-probe-inline.html` with the content element's `animate` and the page's timers replaced by fakes, because the hidden tab freezes both: T1 read the facts text with the fade-out still running, T2 counted 2 fade-outs, 2 hold timers and 2 return swaps for two clicks, T3 showed the panel text at 88px after a stop and re-press, T5 counted 7 tracks for a three-column grid with named lines. T4 was seen with real animations, content at opacity 0. The real extension in a focused tab was not driven, and the tree moved once mid-review, the size lock, which changes none of the findings. | Findings listed | low | e58a763 | Delete the notes file and these two rows. |

| 2026-09-03 | Fast-mode burst: the bubble's language and the confirmation's manners | Run under FAST MODE. All in `tools/rocket-inspector/content.js`. Reading: rows are collected with a `gap()` marker that lands an 8px top margin on the next row that really prints, so an empty group leaves no gap; `fillBubble` re-ordered into four groups, with `Bg` moved down beside Radius, Border and Shadow; labels gained a colon; `spacingRow` drops a zero margin or padding; `capWords` lifts whole lowercase words to Full, None, Solid, Inset, leaving hex, units and `linear-gradient()` alone and never touching the copied card; the colour row is labelled `Text:`; `LH`/`LS` became `L-H`/`L-S`. Confirmation: `lockBubbleSize` pins the bubble's own box as minimums for the panel and `unlockBubbleSize` releases it; the panel is centred both ways through the existing `bubbleDisplay` flex path; the ring grew to 36px with its tick scaled to match, stroke still 2px; the hold went 700ms to 3.2s to 2.4s; new `restoreFacts` and `endFlash` end the confirmation the moment the hover settles on another element. The highlight box dropped to z-index 2147483644 so the band pills clear its outline while the bubble stays on top. | The owner was the reviewer, per the mode: every round was loaded in his browser and passed before the next was asked for. The size lock was measured before the mode began, facts and panel both 214x174 at the same point, and the file was syntax-checked after each patch. | Pass | medium | e58a763 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Fix the review's findings: T2, T3, T4, T5, T6, T8 and P1 | `tools/rocket-inspector/content.js`: new `flashGen`, claimed by `flash` and by `restoreFacts` and bumped by `stopInspect`, lets an overtaken build return `false`, which `swapContent` now honours by leaving the bubble untouched instead of placing and animating it; this closes the stacked double-click (T2) and the stale panel after a release mid-swap (T3) with one mechanism, and also covers a click landing during the return swap, which the review did not name. The rise-in animation gained the fade-out's stall guard (T4). `layoutOf` strips `[line-name]` tokens before counting tracks and ignores `subgrid` (T5). In `idCard`, `flat` moved above the `file:` and `comp:` lines and now wraps both, so no page-written value can carry a newline into the card (T6, including its pre-existing half). Two stale comments corrected and a no-op flex-direction ternary dropped (T8); `FLASH_OK`, `FLASH_FAIL` and `checkIcon` are still inert and still in place, the owner's call. `project-os/History.md`: fourteen scan rows moved verbatim out of the appendix table back to the end of the scan log, three table-breaking blank lines removed (P1). | The real script was stepped on a virtual clock in `.tmp/qa-swap.html`, since the pane's hidden tab throttles timers and freezes animations: two clicks in a row produced two swaps, the overtaken one landed on the facts and changed nothing, the newer one showed the panel, and exactly one hold timer existed where there used to be two; the return left no orphan timer and released the size lock. Key released mid-swap: no panel was placed, no hold timer survived, and the next press showed the newly hovered element's facts, which is T3's exact symptom. Grid: `[full-start] 100px [content-start] 200px [content-end] 100px [full-end]` now reads `cols 100px, 200px, 100px`, and the even cases still read `3 cols 84px` and `4 cols 380.25px`. Both History tables verified to render as one table each, 75 scan rows and 73 appendix rows, no malformed row. T1 needed no work: it was already closed by the earlier `restoreFacts` refactor. T7 was dropped as churn, per the owner. | Pass | medium | e58a763 | Revert this row's hunks in `content.js`; the History move is a line reorder in the same commit. |
| 2026-09-03 | Drop the empty Bg row | `tools/rocket-inspector/content.js`: `showBg` now requires `painted`, and `bgRow` lost its `None` branch. The clause that always gave a button or a link a Bg row went with it, since it existed only to force the empty state; named here because the request superseded it rather than naming it. | Verified in the pane across six elements: a plain div, a flex row and a link with no background print no Bg row and no leftover gap; a painted card reads `Bg: #333222`, a white pill `Bg: #FFFFFF`; an svg is unchanged. The group breaths still land only before rows that really print. | Pass | low | 28c9f59 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Hide a normal line height, reword the no-class label | `tools/rocket-inspector/content.js`, in `fillBubble`: the L-H row is built only when `pxLabel(lineHeight)` is not `normal`; the first row's fallback text changed from `(no class)` to `No class`. `idCard` untouched, so the card keeps `line-height normal` and `classes: (none)`. | Verified in the pane: a pill, a heading, a bare span and a nav link, all with a normal line height, print no L-H row and read `No class` where they have none; a paragraph given `line-height: 1.6` still prints `L-H: 25.6px`. Group breaths still land only before rows that print. | Pass | low | 28c9f59 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Reorder the type group, unlabel the colour | `tools/rocket-inspector/content.js`, in `fillBubble`'s text branch: the colour row moved above the L-H and L-S rows and dropped its `Text: ` prefix, keeping its swatch and its backdrop compositing. The card's `type:` line is untouched. | Verified in the pane: a paragraph given a real line height and letter spacing reads family, `Regular, 16px`, `#000000` with its swatch, `L-H: 25.6px`, `L-S: 0.32px`, in that order; a heading, a button and a text-bearing box show the same order with the rows they own. | Pass | low | 695ec33 | Revert this row's hunk in `content.js`. |
| 2026-09-03 | Round the colour swatch | `tools/rocket-inspector/content.js`, in `drawRows`: the swatch span's radius went from 2px to fully round, its size 12px to 10px and back to 12px, and its vertical alignment -1px to -2px and back to -1px, both at the owner's word. It keeps its 8px left margin and its place after the row's text; the compositing over the page's real backdrop is unchanged. | Verified in the pane on a heading carrying both swatch rows: each swatch measures 10x10 at radius 999px, aligned -2px, with the text before it in the row. | Pass | low | 695ec33 | Revert this row's hunk in `content.js`. |
| 2026-09-03 | Show the element's padding as a wash | `tools/rocket-inspector/content.js`: new `padBand` draws one fixed rectangle in `INSIDE_BAND`, the same colour the gaps between children already use, since padding is internal spacing too; a green tried first in this same round read as a second, unrelated language and was replaced before it shipped. New `drawPadding` measures the four sides and draws only the ones that have padding, returning early when all four are zero. It is called from the top of `drawBands`, after the clear and before the child-gap work, so the rectangles are laid down first and an orange gap always paints above them, and so it also runs for text elements, which the gap code skips. Geometry comes from the rect divided by `offsetWidth` and `offsetHeight`, so page zoom and a transformed ancestor scale the bands correctly. A probe page, `.tmp/padding-options.html`, drew three treatments on light and dark surfaces for the owner to choose from. | Verified in the pane on real computed styles: a pill with 6/14 gets four bands one pixel inside its border; a card with 16 top and bottom gets two, not four; a card with 32/16 gets four at the right thicknesses; elements with no padding get none. Under `transform: scale(0.6)` a 10px padding draws a 6px band that still fits the visual box. Draw order confirmed: the four padding rectangles are appended before the two orange gap bands. The rule-17 review of this change found one defect, fixed and re-verified inside the same task: a wrapped inline has one padding box per line, and the single-box maths would have washed the wrong lines, so an element reporting more than one client rect now draws nothing, 0 bands on a link across three lines, 4 on the same link when it fits one. Screenshot taken of the finished behaviour on a dark card. | Pass | medium | 695ec33 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Re-check T1 to T3 against the fixed code | No code changed. `notes/Code-Review-2026-09-03.md`: T1 to T6, T8 and P1 marked done with the fixing commit; T7 left open. | The probe page was rebuilt from the committed script and driven in the pane with timers and the content's `animate` under control: a mouse move during the return swap left the panel in place with its fade-out still running (T1); two clicks produced one panel, one hold and one return (T2); a click during the return produced one fresh confirmation (the case the fix row named); a key release mid-swap left no hold timer, and the next press showed the element's facts at full width (T3). The only remainder is cosmetic: a double-click still starts two overlapping fade-outs of the same content, so the fade restarts once, inside 144ms. | Pass | low | 695ec33 | Revert the status edits in the notes file and remove these two rows. |

| 2026-09-03 | Bare numbers on the inside bands, padding numbered too | `tools/rocket-inspector/content.js`: new `INSIDE_INK` and `INSIDE_HALO` pairs and an `insideTheme` judged once per paint in `drawBands` from `backdropOf(el)` through a new `isDarkColor`, so the number reads against what really sits behind the band rather than the page's body. New `bandNumber` centres a 12px medium digit on a band, with a three-layer halo only when the band is under 14px tall or narrower than the digits. `addBand` gained a `plain` flag: the child-gap call passes it and gets a bare number, the outward call does not and keeps its blue pill. `padBand` takes the CSS value and numbers each padded side. The stale `one notch quieter` comment on `padBand` was corrected in passing. Probe page `.tmp/number-on-band.html` drew both treatments for the choice. | Verified in the pane on real computed styles: a dark card's numbers came out sand `rgb(240, 214, 170)`, a light row's brown `rgb(111, 90, 58)`; a 32px padding band carried 32 with no halo, a 16px side band carried 16 with one, 4px padding and 12px gaps spilled with halos, and the outward bands still wore pills, `315 24` and `20`. Count of orange bands per element unchanged from the previous row. Screenshot of the dark card taken. | Pass | medium | 695ec33 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Click on the confirmation restarts its hold; T7 rename | `tools/rocket-inspector/content.js`: `flash` starts with a branch for a hold already running, which clears that timer, starts a new one and returns, so the panel is never rebuilt; the 2400 moved into a `FLASH_HOLD_MS` constant used in both places, named here as the one side effect. In `componentOf` the inner `nameOf` became `typeName`, four occurrences, and the outer `nameOf` is untouched. Rule-17 self review: no findings. | Probe page rebuilt from the edited script and driven in the pane with timers and the content's `animate` faked: with the panel up, a second and a third click left the same panel node in place, started no fade-out, and left exactly one hold timer; firing it returned the facts once. Two clicks inside the first swap still yield one panel and one hold. A hand-built fiber on the button read `comp:    SaveButton` and a forwardRef shape on the heading read `comp:    IconLabel`; the parent line still reads `div.card`. Syntax-checked. Not covered: the panel keeps its first outcome, so a copy that fails after one that succeeded still reads `Specs copied!` while the panel is up. | Pass | medium | 695ec33 | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Code review of the four commits since the morning review | New `notes/Code-Review-2026-09-03-B.md`, one reviewer, no fan-out, per the owner. Two important findings: T1, `flash` runs when the clipboard promise resolves, so a key released before that builds the panel into the hidden bubble and the next press inside the 2.4s hold shows it over the new element; T2, `componentOf` gates on a capital letter only, so production React fibers print minified names such as `comp: Ko`. Four nits: T3 a flick to another element inside the 144ms swap keeps the panel at the old element's locked size for the full hold, T4 `subgrid [a] [b]` reads `1 cols subgrid`, T5 a padded image gets washes but no Padding row, T6 the panel keeps its first outcome (already named by the owner). D1, this file: a blank line at 128 breaks the scan table again and the morning review's scan row at 104 sits above the fourteen rows moved back in 28c9f59. Pre-existing P1: `pageTheme` parses rgb() only, so an oklch body reads light. No code changed. | Range e58a763..7759097, HEAD equals origin/main, tree clean. The committed script was driven in the pane on `.tmp/review-probe-2.html` with a virtual clock and faked `animate`: T1 re-press over element B read "Specs copied!" with display flex and one hold timer; T3 read the panel over B with one hold timer and minWidth 214px; T2 hand-built fibers read `comp: Ko` (production shape) and `comp: SaveButton` (dev shape); T4 computed value `subgrid [a] [b] [c] [d]` printed `layout: grid, 1 cols subgrid`; T5 an img with 10px padding drew washes and a bubble without Padding; P1 body `oklch(0.13 0.028 261.7)` printed `view: ... light`. The real extension in a focused tab was not driven. The probe tab was closed. | Findings listed | low | 7759097 | Delete the notes file and these two rows. |

| 2026-09-03 | Fix all findings of the second review: T1 to T6, D1, P1 | `tools/rocket-inspector/content.js`: `flash` returns at once when not inspecting or with no target (T1; the `!target` half is a side effect named for the owner: a click before any mouse move has no bubble either); it remembers the element it is about and, when the cursor has settled elsewhere by the time the swap ends, fills that element's facts instead of the panel and starts no hold (T3); the hold-running branch rewrites the panel in place when the outcome differs, tracked in new `flashOk` (T6). `componentOf` trusts a fiber only when it carries `_debugOwner`, a dev-only field (T2). `layoutOf` treats any value starting with `subgrid` as no track list (T4). `drawBands` skips `drawPadding` for images, matching the bubble's own rule that an image's padding is not news (T5, the smaller of the two options offered). `pageTheme` keeps the exact rgb() path and sends every other colour through `isDarkColor` (P1). `project-os/History.md`: the blank line splitting the scan table removed, the morning review row moved verbatim to after the last 2026-09-02 row (D1). `project-os/BugAtlas.md`: row 1, the stale confirmation class, bitten twice today. `project-os/Code_review.md`: the example block replaced by two real always-check rows, the confirmation never outlives the key, and fiber names come from dev builds only. `notes/Code-Review-2026-09-03-B.md`: all eight marked done. | Probe page rebuilt from the edited script and driven in the pane on a virtual clock with `animate` faked, `.tmp/review-probe-2.html`. T1: release then resolve then re-press over B read B's facts, display block, zero hold timers. T3: click A, settle on B at 60ms, swap end read B's facts, zero holds, size lock released. T2: the production-shaped fiber printed no comp line, the dev-shaped one `comp: SaveButton`. P1: `oklch(0.13 0.028 261.7)` read dark, `rgb(255, 255, 255)` light, `rgb(20, 20, 20)` dark. T4: `subgrid [a] [b] [c] [d]` read `layout: grid`, the plain grid still `cols 100px, 200px, 100px`. T5: a padded img drew 0 orange washes, a padded card still 4. T6: a rejected write after a success read `Failed, try again`, one hold, no swap. Regressions from both earlier reviews re-run and held: double-click one panel one hold, key released mid-swap zero holds and the next press shows facts, click during the return swap one fresh confirmation, click on the panel keeps the same node with one hold, mouse move during the return keeps the panel until the facts land. Syntax-checked with node. Rule-17 review of the diff found the `!target` gap, fixed and re-run. The real extension in a focused tab was not driven. The probe tab was closed. | Pass | medium | 7759097 | Revert this row's hunks in `content.js`; the History move is a line reorder, the atlas and review rows are additions. |

| 2026-09-03 | Rebuild the bands only when the element or its box changes | `tools/rocket-inspector/content.js`: new `bandsFor` and `bandsBox` remember which element the bands on screen belong to and its box at draw time; `paint` calls a new `redrawBands` only when either differs, `hide` forgets them, and the scroll and resize listeners go through the same helper so the memo stays true. The 60ms settle before a new element is adopted is untouched. | Measured in the pane with 4000 extra nodes on the page so a layout costs something: a mouse move over the same element took 2.43ms before and 0.07ms after. The redraw still fires when it should: giving the hovered element padding and moving the mouse once replaced the band nodes and drew the four new ones. Three earlier probes, one per recent commit, showed the JS cost itself never rose above about 1ms on a small page, which is why the fix went after the per-move rebuild rather than any one new line. | Pass | medium | 7dbcffd | Revert this row's hunks in `content.js`. |
| 2026-09-03 | Backlog the nine bubble ideas | `project-os/Backlog.md`: the empty placeholder row in Open replaced by nine rows, one per idea, all sourced to the same chat pass. No code touched. | Table shape checked by eye: header, separator, nine three-column rows. | Pass | low | e52c6b4 | Delete the nine rows and restore the placeholder. |
| 2026-09-03 | Settle from entry, and one layout per hop | `tools/rocket-inspector/content.js`: `aimAt` gained `settleFor`, the element the running wait is counting for, and returns early when the same element keeps sending moves, so the timer runs to its end; a different element still restarts it. `drawOutward` caches each container's sibling rects in a map shared by the four directions and collects the band geometries in `pending`, appending them only after every direction is measured. `SETTLE_MS` itself is untouched at 60. | On the stepped clock, moving inside a new element every 16ms: the bubble switched at 64ms and stayed, where before it never switched while the mouse moved; a sweep on to a third element mid-wait restarted the count and switched 64ms after entering it. On a page with 3000 nodes and a 300-sibling row, a hop measured 0.2 to 0.4ms where it measured 8 to 13ms before, and the outward band on the chip still draws. Rule-17 review of the diff found nothing: `stopInspect` clears the timer, which disarms the new early return by itself, and the outward edges are still remembered before the bubble is placed. | Pass | medium | 7f07e50 | Revert this row's hunks in `content.js`. |
| 2026-09-04 | Step under an invisible cover overlay | `tools/rocket-inspector/content.js`: new `isCoverOverlay` is true only for an absolutely or fixed positioned element with no background, no background image, no shadow, no painted border and no words of its own, whose box covers at least 90 percent of its parent's. New `resolveTarget` walks `document.elementsFromPoint` past every such layer, and hands whatever it finds, or the cover's own parent when hit testing reports nothing real, to a new `deepestAt`, which descends by box geometry to the deepest visible element containing the cursor. Geometry is what makes the owner's own card work: its content carries `pointer-events: none`, so hit testing never mentions the title at all. The layer stepped under is remembered in `coveredEl` and `coverEl`; the mousemove handler, the click handler and `startInspect` all pick through it, so the bubble and the card always agree. `idCard` gained an `overlay:` line, after `parent:`, naming the layer's tag, classes and aria-label, and only for the element it actually covered. Three cases were added to the QA page: the stretched-link card, a wrapping link, and a positioned panel that paints. | Verified in the pane by feeding the picker the element stack a real browser returns, since the pane's own window measures 0 by 0 and `elementsFromPoint` is empty there. Over the title under the stretched link the bubble showed the H5; over a blank part of the card it showed the card; the wrapping-link case still showed the inner div; the painted positioned panel was not skipped. The copied card for the title carried `overlay: a · absolute inset-0 rounded-card · aria-label=Dig · covers this element`, and an ordinary element's card carried no overlay line. The owner then reported the texts still unreachable on his own site; the QA case grew a `pointer-events: none` row inside the card, reproducing it, and with the browser reporting only the overlay the picker now lands on the title, on the dots beside it and on the notes line, while a blank part of the card still gives the card. Regression sweep after the descent landed: the wrapping link, the painted panel, a heading, a button, a stat card and a pricing card all resolve exactly as before. | Pass | medium | 87d8704 | Revert this row's hunks in `content.js`. |
| 2026-09-04 | Four more cover shapes | `tools/rocket-inspector/content.js`: `isCoverOverlay` split its area test into `coverRatio`, gained `isSvgCover` for a shape inside an `<svg>` that fills and strokes nothing while covering the drawing, and now treats a positioned layer at zero opacity as a cover whatever it would paint if shown. New `coversByPseudo` catches the Bootstrap and Tailwind pattern by its signature: the element the browser hands back does not contain the cursor in its own box, and it has an absolutely positioned `::after` or `::before`; `resolveTarget` sends that case into the same geometric descent. `deepestAt` now also walks an open `shadowRoot`'s children, which are not children of anything. Four cases were added to the QA page: the pseudo-stretched card, a chart with a catcher rect, a hover veil over an image, and a web component built at test time. | Verified in the pane by feeding the picker the element a real browser hands back for each pattern: over the pseudo-stretched card the bubble reached the title and the notes; the chart catcher resolved to the bar actually under the cursor, 120 × 20 and 200 × 20 for the two; the veil resolved to the image, 280 × 100; the shadow tree's span was reached from a cover in the light DOM. The link's own words still resolve to the link, so the pseudo rule does not swallow it. Regression sweep over eleven ordinary elements, unchanged. | Pass | medium | 87d8704 | Revert this row's hunks in `content.js`. |