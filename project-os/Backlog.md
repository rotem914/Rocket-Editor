# Rocket Editor — Backlog

The owner-curated open-items list. One place to see what is still open.

## How you maintain this file

- **Nothing enters this file on your own initiative.** An item is added only when
  Rotem says so. Your own good ideas are not backlog items — offer them
  in chat and let them decide. An assistant-curated backlog fills with noise, and
  a noisy list stops being read.
- Verdict vocabulary on any finding you report: **fix** (do it now), **drop**
  (never mind), **backlog** (add a row here). Only `backlog` writes to this file.
- One row per item: date added · the item in plain words · where it came from.
- Plain words, no jargon. This list is read by the owner, not by you.
- **Scan the Open table at task pickup.** If the current task touches an open
  item, say so in chat before you start.
- When an item is done, **move its row to Done with the closing date**. Never
  delete a row — the record of what was decided is part of the value.
- Full detail stays in the source it came from. This file stays one line per item.

## Open

| Added | Item | Source |
|---|---|---|
| 2026-09-03 | Inspector bubble: show the variable behind a value, `#2D41D7 · --primary`, read from the matched rule, so Claude gets the real address and not a hex | Chat, 2026-09-03, the designer's-hat pass over the Inspector bubble, all nine backlogged at once |
| 2026-09-03 | Inspector bubble: a layout line for a container, `Flex row · gap 12` or `Grid 3 cols · gap 16/24`, so spacing research does not need the copied card | Chat, 2026-09-03, the designer's-hat pass over the Inspector bubble, all nine backlogged at once |
| 2026-09-03 | Inspector bubble: uppercase, italic, underline, alignment and opacity, one line each and only when set, so the bubble says what the eye sees | Chat, 2026-09-03, the designer's-hat pass over the Inspector bubble, all nine backlogged at once |
| 2026-09-03 | Inspector bubble: flag `Fallback` next to the font family when the first family did not load and a substitute is rendering | Chat, 2026-09-03, the designer's-hat pass over the Inspector bubble, all nine backlogged at once |
| 2026-09-03 | Inspector bubble: a `Max-W` line when a width limit is set, so a section that stops growing explains itself | Chat, 2026-09-03, the designer's-hat pass over the Inspector bubble, all nine backlogged at once |
| 2026-09-03 | Inspector bubble: contrast ratio of the text colour against its backdrop, `4.6:1` | Chat, 2026-09-03, the designer's-hat pass over the Inspector bubble, all nine backlogged at once |
| 2026-09-03 | Inspector: pin one element with a held key, hover another, and the bubble shows only the differences between the two | Chat, 2026-09-03, the designer's-hat pass over the Inspector bubble, all nine backlogged at once |
| 2026-09-03 | Inspector: a click on a bubble row copies that one value; a click anywhere else still copies the whole card | Chat, 2026-09-03, the designer's-hat pass over the Inspector bubble, all nine backlogged at once |
| 2026-09-03 | Inspector bubble for images: natural size against rendered size, `2400 × 1600 → 800 × 533`, and the fit mode | Chat, 2026-09-03, the designer's-hat pass over the Inspector bubble, all nine backlogged at once |

## Done

| Added | Closed | Item | Source |
|---|---|---|---|
| | | | |

